{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "T42: the server-backed application window gets only baseline window/event capability. SQL, dialog, filesystem, shell, and custom-command access are deliberately absent, so loaded remote content cannot obtain a generic native bridge.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
