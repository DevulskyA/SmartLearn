# SmartLearn V1 consolidated validation - initial state

PLAN STATUS: AUTHORED_FOR_USER_ACTIVATION
PRODUCT IMPLEMENTATION STATUS: NOT_EXECUTED_BY_THIS_PACK
CURRENT EXECUTOR CANDIDATE: TO_BE_RECONCILED_AT_T01
FORMAL TLC EXECUTION VALIDATORS: NOT_RUN_IN_THIS_AUTHORING_DELIVERY
INDEPENDENT AUTHORING VERIFIER: NOT_AVAILABLE; no independent-verification claim

This file starts honestly incomplete. The executor updates it with actual commands/results as T01-T54 proceed. Never copy historical results into new candidate PASS cells without a justified source/tree equivalence check.

## Evidence actually obtained while authoring

- Read the remote eight-branch inventory pinned in context.md.
- Read the entire historical learning-core.js, its nine tests, its spec/design/validation/contract and relevant governance changes.
- Reconstructed the source/test files from connector reads; Git blob hashes match the cited historical blobs.
- Ran the historical nine focused tests: 9/9 PASS in Linux, Node v22.16.0.
- Executed ten characterization probes; all ten reproduced the outputs in evidence/adversarial-results.json. These expose limitations/defects in the historical prototype; they do not certify it.
- Read the real current planUnitSaveBtn implementation and the continuation E2E file. The existing single-save path is present; the recent two-button test did not exercise it.
- Authored a forward dependency graph of 54 tasks, mapped to 24 requirements and 28 acceptance criteria. Structural plan checks are recorded in PLAN_QA.json; they are not TLC product completion checks.

## Historical baseline, not rerun here

Source baseline: main f645a0730f6e37560de813b1610f359a57419f27.
Executor previously reported client 223/223, server 17/17, Rust 13/13, build, process smoke and independent verifier PASS. Runtime results are reports of that baseline, not a claim that the authoring environment reran them or that later V1 changes passed.

## Fill at execution

| Gate | Status | Candidate/environment/command/evidence |
| --- | --- | --- |
| Worktree/authority reconciliation (T01) | PASS | Continuation `claude/smartlearn-v1-complete` before=18e903b2f297a251764b8e37a567a0fef1634664 (215c89d + E2E harness, pre-integration). `git merge origin/main` (f645a0730f6e37560de813b1610f359a57419f27, containing 23358b6 PR#3 + 215c89d PR-1 via merge-commits) -> after=e5292fa03ef29efa4bf0c90d640ba9699192f133, clean ort merge, only diff: deletion of `data/smartlearn-backup-importavel.json` (4015 lines), confirmed via `git log --oneline -- <path>` to be the same legacy non-medical dataset main had already deliberately removed (commit da4684b "chore(data): remove legacy non-medical backup dataset") — not new data loss. Then materialized consolidated-v2 pack (62ea681) + heritage.md + T02/T45/T49/T52/T54 amendments (bfbb82b). `git merge-base --is-ancestor 23358b6.. HEAD` and `...215c89d.. HEAD` both true. `npm test` post-merge: 223/223 PASS. Sibling worktree `fix-complete-review-sqlite-593426` (PR#3 branch) observed DIRTY with another session's uncommitted work (.claude/loop.md, docs/research/, .specs tasks.md status edit) — recorded, not touched, not merged. |
| Source and intended test inventory (T02) | PASS | Heritage Coverage Matrix filled in heritage.md with real repo evidence: 7/9 rows PROVEN (discipline catalog, master study entry, automatic schedule, daily decision surface, discipline performance, unit evolution, longitudinal tracking), 2/9 PARTIAL with explicit GAP linked to future tasks (execution results — item-level evidence GAP -> T29-T33; backup/recoverability — server migration GAP -> T25-T28). No row silently dropped. STATE.md updated with current active-plan handoff, old PR-0/PR-1 checkpoint preserved below as historical evidence per design.md §1. |
| Test discovery / CI (T05) | PASS | Confirmed no hidden/nested test files via find (only test/, server/test/, e2e/ contain *.test.js/*.spec.js). Created scripts/check-test-inventory.mjs (walks repo excluding node_modules/.git/dist/.claude/evidence/.specs, verifies every *.test.js/*.spec.js is under one of the three known discovery roots) as `npm run test:inventory` — PASS, 18 files (13 root + 4 server + 1 e2e), zero orphans. Created .github/workflows/ci.yml (checkout, Node24, npm ci both root+server, inventory check, root test, server test, build, Playwright E2E; separate Rust job with Tauri Linux deps + cargo test --locked). NOTE: actions pinned by major-version tag (@v4/@stable), not yet by exact reviewed SHA — recommend SHA-pinning before first authorized push, per T05's own instruction; recorded honestly, not silently satisfied. CI remote result is UNVERIFIED (no push performed, none authorized). One `test.skip()` exists (AC-04 in e2e/smartlearn-plan-flow.spec.js) with an explicit BLOCKED_EXTERNAL annotation, not hiding a required criterion. Full gate rerun: npm run test:inventory PASS, npm test 231/231, npm --prefix server test 17/17 (after `npm install` — server/node_modules had never been installed in this worktree; environmental, not a product defect), npm run build PASS, cargo test --locked 13/13, npm run test:e2e 12/12. |
| Client/shared suite | PASS | `npm test` on claude/smartlearn-v1-complete @ 8dd276b+: 223/223 PASS, no regression from E2E harness rewrite |
| Target-proven E2E (T03) | PASS | Rewrote e2e/smartlearn-plan-flow.spec.js (11 tests, `npx playwright test`, all PASS). Key fix: prior test clicked the subject-subform's own "Adicionar" submit before "Salvar aula" (two clicks) and asserted `length>0` on seeded data — both were test defects. New AC-03 test types the new subject name, does NOT click "Adicionar", clicks ONLY #plan-unit-save-btn once, and asserts exact deltas: +1 subject row (by name, deduped), +1 learning_unit row (by subjectId+title), +16 review_task rows (by unitId) — confirming src/app.js:3347 handler + src/db.js:1464 `newSubjectName` atomic path is real and already compliant with AC-03. Added preflight() (title="SmartLearn" + exact 6-screen data-screen set + #plan-unit-save-btn existence) run in beforeEach before any localStorage write, satisfying AC-02. AC-04 (injected mid-write failure) recorded BLOCKED_EXTERNAL — no window-exposed DB hook exists to inject a failure without a test-only product seam; deferred to T13+ server-side transaction test instead of fabricating a pass. |
| Server suite and real SQLite (T06) | PASS | Discovered gap during setup: server/src/main.js had no SIGTERM/SIGINT handler (process would terminate without explicitly closing the Fastify app or the better-sqlite3 handle). Added a graceful-shutdown handler (app.close() + db.close() + process.exit(0), guarded against double-invocation). Created server/test/process-smoke.test.js (4 tests, real child_process, real HTTP, real TempDir): (1) server starts and reaches /health/ready when spawned from a cwd other than server/, proving path resolution is cwd-independent; (2) a second instance on an occupied port exits nonzero with EADDRINUSE in output, not a hang; (3) restart on the same DB file after the first instance fully exits reaches /health/ready again, proving the file was not corrupted/locked; (4) SIGTERM — on POSIX asserts exit 0 + "Shutdown complete" log; on win32 (this environment) asserts only that the process still exits, with an explicit code comment recording that Node does not support real signal delivery on Windows (child.kill('SIGTERM') unconditionally terminates), so the graceful code path itself is exercised by code review, not by OS-level signal proof, on this platform — recorded honestly rather than asserting a Unix guarantee that cannot be verified here. All 4 new + 17 existing server tests: 21/21 PASS, repeated 3x consecutively (no native crash, better-sqlite3 13.0.3 confirmed stable — reconfirms PR-1 closure finding on this candidate, not just historically). Root suite 231/231 PASS, no regression from the main.js change. |
| Native Rust suite | NOT_RUN | |
| Production Web build | NOT_RUN | |
| Target-proven E2E | NOT_RUN | |
| Unified Unicode validation (T04) | PASS | Extended `NAMING_PATTERN` in src/naming-validation.js to accept en/em dash (U+2013/U+2014), Greek letters (U+0370-U+03FF), superscripts/subscripts (U+2070-U+209F + legacy ¹²³), and µ ± × ÷ < >, while still rejecting sentence-signal ASCII symbols (@ # $ % ^ \` ~ \| \\) and curly quotes — surgical extension, not full allowlist removal, per T04 "smallest sufficient change". Verified via node -e against exact AC-05 corpus (Na⁺/K⁺-ATPase, β-bloqueador, O₂, µg, pH < 7,35, em-dash) before editing tests. Updated test/naming-validation.test.js (Contract C/D/H) to assert acceptance instead of rejection for em-dash/Greek/superscripts, kept rejection assertions for @ and control chars. Updated e2e KNOWN GAP test to AC-05 PASS test with exact round-trip assertion. 68/68 naming-validation unit tests PASS, 231/231 full root suite PASS (was 223, +8 new positive-case tests), 12/12 E2E PASS (was 11, +1 new sentence-signal-still-rejected test). |
| Strict /v1 HTTP envelope (T07) | PASS | Created server/src/http-contract.js: applyDomainEnvelope(app, {resolveActor}) — default-deny actor resolution (fails closed to null until T09/T10 injects a real session lookup via DI, never trusts client-supplied body.userId/headers), AJV compiled with additionalProperties:false (rejects unknown fields and wrong types, never coerces), {error:{code,requestId,field?}} envelope on every path (validation/401/404/500), request-id generated or propagated via x-request-id header. Registered as an encapsulated Fastify sub-context at /v1 prefix in server/src/app.js — /health/live and /health/ready registered on the root app stay outside the envelope (public, minimal), confirmed via real process: curl /health/live=200, /health/ready=200, /v1/anything=401 (unauthenticated deny, no real session mechanism exists yet). server/test/http-contract.test.js: 11/11 PASS covering AC-06 (client userId rejected as unknown field), AC-07 (401 default-deny, including on 404s so route topology isn't leaked to anonymous callers), AC-08 (unknown fields/wrong types rejected; unexpected errors never leak SQL/paths/stack in body). Full server suite 32/32 (21 existing + 11 new), root 231/231 (no regression), test:inventory PASS (20 files now, was 18). |
| Owned accounts + session schema (T08) | PASS | Added server/migrations/002-identity.sql (users: normalized+display email, password hash/salt/algorithm/params, no plaintext; sessions: SHA-256 token_hash unique, user_id FK, issued/expires/last_seen/revoked_at). server/src/repositories/{users,sessions}.js: safe DTOs only (password fields never leave users.js except via findByEmailWithSecrets for the future login path). Strengthened db.js synchronous NORMAL->FULL per design.md §2 scoped decision (now that authoritative user data exists). server/test/identity-schema.test.js (9 tests): tables created, idempotent rerun, upgrade path from a 001-only foundation DB (byte-identical fixture content — verified this by first debugging a false EPERM that was actually a checksum-mismatch exception silently skipping db.close()), broken migration in the identity range rolls back fully, duplicate email -> stable EMAIL_CONFLICT code (not raw SQL error), session FK violation -> stable INVALID_USER code, safe DTO never serializes password fields, WAL/FK/synchronous=FULL all confirmed active, session create/find-active/revoke round trip. Fixed 2 stale tests discovered by this change: server/test/db.test.js expected synchronous=NORMAL (updated to FULL with rationale comment); server/test/migrations.test.js asserted `COUNT(*) = 1` on schema_migrations (broke the moment 002 was added) — rewrote to check version=1 count and pre/post-rerun delta instead of a hardcoded total, so it stays correct as more migrations are added. Full server suite 41/41 (x3 consecutive, no native crash), root 231/231, build PASS, real-process smoke against migration 002 + synchronous=FULL confirmed /health/ready=200. |
| Bounded password authentication (T09) | PASS | server/src/auth/passwords.js: async node:crypto scrypt (N=131072,r=8,p=1,keylen=64,maxmem=256MiB per design.md §3), fresh random 16-byte salt per hash, algorithm/params persisted as JSON for forward-readable rehash, timingSafeEqual comparison over equal-length buffers, bounded concurrency (2 concurrent jobs + FIFO queue — unbounded scrypt concurrency is a trivial DoS on the event loop), runDecoyHash() pays the identical scrypt cost for unknown-account login attempts (no username enumeration via timing). validatePasswordShape: min 15 Unicode codepoints, max 1024 UTF-8 bytes, rejects null/non-string without throwing; hashPassword never trims/normalizes/truncates (explicit test: trimmed variant of a padded password fails verification, proving the untrimmed bytes were what got hashed). server/src/routes/auth.js: POST /v1/auth/register and /v1/auth/login, both marked `public` (bypass T07's default-deny — cannot be authenticated before an account/session exists), returning only safe user DTOs; no session/cookie issuance yet (reserved for T10). server/test/passwords.test.js (12/12) + server/test/auth-routes.test.js (8/8): unique salts per hash, correct/wrong verification, Unicode passphrase round trip, no plaintext/hash/salt leak in any response body, duplicate email -> 409 EMAIL_CONFLICT, unknown account and wrong password return the identical INVALID_CREDENTIALS code with comparable real elapsed time (both ~200ms+, both paying one real scrypt cost — not a strict timing bound, avoids CI flakiness), bounded/overloaded concurrent hashing all settle without hang/crash. Verified via a real running process: POST /v1/auth/register and /v1/auth/login against a real HTTP server return the expected safe DTO. Full server suite 61/61, root 231/231, test:inventory PASS (23 files). NOTE: rate-limiting of repeated auth failures (design.md §3 "10 failed/15min per account, 60/15min per IP") is explicitly deferred to T10 (bundled there with CSRF/session revocation in the plan) — not silently satisfied here. |
| Sessions, CSRF, revocation (T10) | PASS | server/src/auth/session-tokens.js: 32-byte random opaque token (base64url), SHA-256 hash persisted (raw token never stored), 30-day absolute lifetime + 7-day inactivity window (re-derived from last_seen with an injectable clock, not wall-clock-only), cookie HttpOnly+SameSite=Lax+Path=/, Secure+`__Host-` prefix in production, distinct non-Secure dev name. server/src/auth/csrf.js: 32-byte synchronizer token, constant-time match, exact-origin allowlist (fails closed on missing/null/unexpected Origin), applies to POST/PUT/PATCH/DELETE only. server/src/auth/rate-limit.js: time-windowed (not permanent) per-key failure counters — deliberately time-limited so an attacker cannot lock out a victim account indefinitely by spamming failed logins against their email; wired into login as both per-account (10/15min) and per-IP (60/15min) per design.md §3 defaults. server/migrations/003-session-csrf.sql adds sessions.csrf_token (002 was already committed, so this is a new migration rather than an edit to an applied one). server/src/auth/resolve-actor.js: real session-cookie resolveActor wired into T07's envelope, replacing the fail-closed default — reads cookie, verifies active+non-expired+non-inactive, touches last_seen, revokes on inactivity detection. server/src/routes/auth.js extended: /v1/auth/me (bootstrap: user + csrfToken), /v1/auth/logout (revokes session, clears cookie), /v1/auth/password (verifies current password, rejects on failure without revoking the current session, hashes+persists new password, revokes every OTHER session via revokeAllForUser exceptId, current session survives). http-contract.js gained the origin+CSRF preHandler (runs before the deny check, applies even to public routes like register/login since exact-origin validation is required there too per design.md §3). server/test/session-security.test.js (11/11): token/hash properties, cookie attributes per environment, injected-clock inactivity boundary, CSRF constant-time match, origin fail-closed, rate-limiter block/expire/reset (proving no permanent lockout). server/test/auth-routes.test.js (15/15): full register->login->me->password-change->logout lifecycle over real HTTP with real cookies, CSRF-missing rejection (403) on an authenticated mutation, password change revokes only OTHER sessions (verified: session that changed the password stays valid, a second concurrent session is denied afterward, old password stops working, new password logs in), failed password-change attempt does NOT revoke the current session, cross-account session isolation (session A never resolves to account B). Fixed regressions this integration caused: server/test/http-contract.test.js's existing POST tests needed an Origin header once origin-checking was added (3 tests) — added, plus 3 new tests proving the origin/CSRF guard itself (missing Origin on a public route -> 403, wrong Origin -> 403, GET never subject to the check). buildApp() became async (registers @fastify/cookie) — updated all 5 call sites (main.js, health.test.js x4). Verified end-to-end against a real running process: register -> login (Set-Cookie captured) -> /auth/me with cookie returns user+csrfToken -> /auth/me without cookie returns 401. Full server suite 82/82, root 231/231, test:inventory PASS (24 files). NOTE: the operator reset-token CLI mentioned in design.md §3 ("fixture-tested operator reset-token CLI for authorized recovery") is not yet implemented — recorded as an open item, not silently folded into this task's PASS. |
| Minimal account experience + recovery (T11) | PASS | Design dependency discovered and documented: the Web app still reads/writes only BrowserStore (no server calls anywhere in src/app.js before this task) — real cutover is T20-T24. Implemented the account UI as a self-contained, independently testable module rather than gating the existing learning screens: src/auth-ui.js (fetch client with credentials:'include' for the httpOnly cookie, CSRF header injection, pt-BR error mapping via new src/i18n/{index,locales/pt-BR}.js), a new "Conta" screen in index.html (login/register/logout/password-change forms, reusing existing form-card/field-group styles), and wiring in src/app.js's showScreen()/renderAccount(). Server-side: server/src/auth/reset-tokens.js (single-use, 30-min-lifetime, SHA-256-hashed reset tokens keyed in server_meta) + server/scripts/reset-password.mjs (operator CLI — no unauthenticated HTTP path issues a token; the CLI requires shell access to the server, i.e. explicit operational authorization) + POST /v1/auth/reset-password (consumes an operator-issued token, revokes ALL sessions on success, generic error for invalid/used/expired to avoid leaking token state). Added @fastify/cors (exact origins, credentials:true, no wildcard) since the Web origin and API origin now genuinely differ. Two real bugs found and fixed while building the real E2E (not app.inject) journey: (1) browsers treat 127.0.0.1 and localhost as different sites for cookie purposes — a session cookie set by a 127.0.0.1-hosted server was never sent back to a localhost-origin fetch, so the E2E server and the SMARTLEARN_ALLOWED_ORIGINS config must use the same hostname as the Web origin, not just "any loopback address"; (2) auth-ui.js sent Content-Type:application/json on bodyless requests (logout), which Fastify correctly rejects as FST_ERR_CTP_EMPTY_JSON_BODY — fixed to omit the header when there's no body. test/auth-ui.test.js (7/7, mocked fetch): error-code-to-pt-BR mapping, CSRF header inclusion, credentials:'include' on every call, unknown codes fall back to a generic message rather than leaking the raw code. server/test/reset-password.test.js (6/6): token single-use, expiry, unknown-account returns null (no enumeration via the CLI itself), full HTTP round trip (reset revokes all sessions, old password stops working, new one logs in), invalid token returns a generic error without password-validation details. e2e/auth.spec.js (5/5, REAL server process + real browser, not app.inject): full keyboard register->login->account-view journey, wrong-password error has no sensitive existence leak, logout persists across reload, password change via the actual UI (old password stops working, new one logs in), and explicit proof the existing learning screens (Hoje/Plano) remain reachable and functional independent of login state — the account UI does not gate anything yet. Fixed one pre-existing E2E test (smartlearn-plan-flow.spec.js's preflight) that hardcoded the exact 6-screen set — correctly caught the new 7th "account" screen as a surface change requiring conscious update, not a silent pass. Full final gate: root 238/238 (was 231, +7 auth-ui unit tests), server 88/88 (was 82, +6 reset-password), rust 13/13, build PASS, full E2E suite 17/17 (12 plan-flow + 5 auth). |
| Auth abuse limits + security review (T12) | PASS (see also independent verifier row below) | Added config.trustProxy (default false, env SMARTLEARN_TRUST_PROXY) wired into Fastify's own `trustProxy` option — confirmed via test that Fastify's request.ip already ignores X-Forwarded-For when this is false (the safe default), so per-IP rate limiting cannot be evaded by header spoofing without a misconfigured deployment explicitly opting into a proxy topology. Fixed a real bounded-memory bug found while testing: the rate limiter's original eviction picked the oldest-by-original-insertion-order bucket, which is backwards for an actively-attacked key — a key hit repeatedly was the FIRST evicted once unrelated one-off keys filled the map, because it was also the first ever inserted. Fixed by re-inserting (touching) a bucket on every recordFailure, making Map iteration order genuinely LRU (least-recently-touched), not just insertion order. server/test/auth-abuse.test.js (7/7): memory bounded under 500 distinct keys with a 100-bucket cap, an actively-hit key survives LRU eviction pressure from interleaved noise (a bug in my own first test version — non-interleaved touches "before" noise didn't actually exercise LRU order — fixed the test), expired buckets are swept before evicting a live one, spoofed X-Forwarded-For across 12 login attempts still trips rate limiting (proving the account-level key, which never depends on IP, can't be evaded by IP spoofing regardless of trustProxy), concurrent login storm (8 parallel) settles without hanging (T09's bounded scrypt concurrency holds under real HTTP load), and an explicit spoofed-header attempt (`x-user-id`, `x-actor-override`) proves session resolution never reads anything but the verified cookie. Full gate: server 95/95 (was 88, +7), root 238/238, test:inventory PASS (28 files). |
| Identity/CSRF/cross-user security — independent verifier round 1 | VERIFIER_INDEPENDENT=YES, VERIFIER_RESULT=FAIL @ e779d6b | Independent subagent (genuinely separate context, read-only, did not fix anything) reproduced all test counts itself (server 95/95, root 238/238, e2e 17/17 — not trusted from validation.md) and cleared 11 of 12 adversarial probe areas with concrete code citations (default-deny actor resolution, no client-controlled actor override, CSRF/origin ordering with no bypass route, scrypt params/salt/constant-time match design.md exactly, no raw token ever logged/returned/stored, correct per-environment cookie attributes verified in the actual setCookie call site, no SQL string concatenation anywhere in the auth path, reset-token single-use/non-replayable, no cross-user data leak via any header trick). FOUND A REAL EXPLOITABLE DEFECT (probe 7, rate limiting): server/src/routes/auth.js's login handler called `runDecoyHash()` (a real scrypt computation) for any body that merely failed `validateEmailShape` BEFORE either the per-account or per-IP rate-limit bucket was even checked — an unauthenticated remote client sending `{"email":"x","password":"x"}` repeatedly triggers unbounded real scrypt work through the shared 2-slot worker pool without ever tripping the "10/15min per account, 60/15min per IP" caps described in design.md §3. Compounding this, `/auth/register` had zero rate limiting of any kind despite also calling `hashPassword()` unconditionally, and the shared scrypt job queue (`server/src/auth/passwords.js`) had no maximum length, so the exploit also grows memory without bound. Concerning-but-not-blocking (not folded into the FAIL verdict): no rate limit on `/auth/reset-password` token-consumption attempts (impractical to exploit given 256-bit token entropy, but inconsistent with the posture elsewhere); `/auth/register`'s 409 EMAIL_CONFLICT is a mild account-enumeration oracle (standard registration UX, not something design.md prohibits); `resolve-actor.js` writes `last_seen` on every authenticated GET including polling (write-amplification note, not a security defect). |
| Identity/CSRF/cross-user security — round 2 verifier (fix confirmation) | VERIFIER_INDEPENDENT=YES, VERIFIER_RESULT=PASS | Second, separate independent subagent reviewed the fix commit specifically (traced the exact code path line-by-line for the original exploit, re-ran the full server suite itself — 121/121 — and the targeted auth-abuse file, then went further than reading code: wrote a throwaway script, spawned the REAL server/src/main.js process against a real port and temp DB, fired 20 real HTTP requests with a malformed email, and empirically measured the block transition — 571ms average before request #10, 12ms average from #10 onward, exactly matching DEFAULT_ACCOUNT_MAX_ATTEMPTS=10 — then deleted the script, confirmed via git status no residue). Confirmed: rate-limit checks run before any hashing call in login; type-confusion inputs (non-string email) are rejected for free by AJV schema validation before the handler even runs; /auth/register's limiter counts every attempt (including against already-registered emails) before hashPassword; the queue cap rejects synchronously with no unhandled-rejection risk and maps to a real 503; trustProxy defaults false so header spoofing is inapplicable; the rate-limit reset-on-success path cannot be abused since it only fires after a correct password is already proven. FOUND ONE NEW LOWER-SEVERITY GAP: /auth/password (authenticated password change) called verifyPassword() unconditionally on every request with zero per-route rate limiting — same class of bug, lower severity since it requires an authenticated session (no longer an unauthenticated remote DoS), relying only on the shared queue's 100-slot backstop. Fixed immediately: added a per-userId rate limiter (10/15min) to /auth/password, checked before verifyPassword, reset only on a correct currentPassword. New test in auth-abuse.test.js confirms a 13-attempt wrong-currentPassword flood via real HTTP produces a fast (<100ms) response once blocked, proving no scrypt is attempted post-block. Full gate after this final fix: server 132/132 (was 121, +11: T15's 10 + 1 new security test), root 240/240 (T15 added 2 calendar-validation tests), test:inventory PASS (31 files). | Reordered server/src/routes/auth.js's login handler: both the per-IP key (always available) and a defensively-coerced per-account key (derived from the raw client-supplied email BEFORE shape validation, so a malformed email can't dodge having a key at all) are checked against the rate limiter FIRST, before any shape validation, DB lookup, or scrypt/decoy-hash call. Added a matching per-IP limiter to `/auth/register` (20/15min, counting every attempt — successful or not — since each one costs one real scrypt call), returning 429 once tripped. Added a hard `MAX_QUEUE_LENGTH=100` cap to the shared scrypt worker queue in `passwords.js` as an independent second backstop (`HashQueueOverloadedError`, mapped to HTTP 503 in http-contract.js's error handler) — bounded memory here no longer depends on every current and future route getting its own limiter exactly right. Also fixed a real test-isolation bug discovered while verifying the fix: `loginRateLimiter`/`registerRateLimiter` were module-level singletons shared across every `buildApp()` call in the same process, which is correct for a real single-process server but leaked rate-limit state between independently-constructed test apps sharing the same `app.inject()` fake IP — moved both limiters to be created fresh inside `registerAuthRoutes()` per call. New tests in server/test/auth-abuse.test.js (3 added, 10/10 in the file): a 15-attempt malformed-email flood confirms the post-block request returns in <100ms (proving no scrypt was attempted, not just that it eventually 401s); a 25-attempt registration flood confirms 429 once the cap is hit; a 250-job direct call to `hashPassword()` (bypassing HTTP entirely) confirms the queue cap itself rejects with the specific `HASH_QUEUE_OVERLOADED` code once exceeded while jobs within the cap still succeed. Full gate after the fix: server 121/121 (was 95 pre-T13/T14, includes the 3 new security tests + T13's 11 + T14's 12), root 238/238, rust 13/13, build PASS, full e2e 17/17, test:inventory PASS (30 files). A second independent verifier pass against this fix is required before this row can read VERIFIER_RESULT=PASS — self-assessment of a fix to a defect the same session introduced is not sufficient closure. |
| Owned learning-domain schema (T13) | PASS | Naming note: renumbered the plan's suggested "003-learning-domain.sql" to server/migrations/004-learning-domain.sql because 003 was already committed for T10's session CSRF column — migrations are append-only, never edited/renamed once applied (design.md §1). Created subjects, learning_units, review_tasks, exercises, exercise_versions, learning_evidence, user_settings, all with user_id NOT NULL and UNIQUE(user_id, id) on the parent side. Child tables use COMPOSITE foreign keys (user_id, parent_id) REFERENCES parent(user_id, id) — this makes cross-user linkage a database-level impossibility: a row can only reference a parent with the identical user_id, so even a caller who knew another user's raw numeric id could never construct a valid FK row. Verified this is real, not just a comment: server/test/domain-schema.test.js directly attempts a cross-user learning_units->subjects insert and a cross-user review_tasks->learning_units insert with correct-but-foreign raw IDs — both throw FOREIGN KEY constraint errors (11/11 tests, no application-layer bypass exists because the constraint is enforced by SQLite itself with foreign_keys=ON, confirmed already active since PR-1). learning_evidence CHECK constraints enforce questions_count>0 and correct_count<=questions_count at the DB boundary (fail closed, not just in service code) plus a type enum (REVIEW/INITIAL_PRACTICE/EXTERNAL). NULL review_task_id (external/initial practice, not tied to a scheduled review) verified valid regardless of owner, per SQLite's MATCH SIMPLE composite-FK semantics. subjects.UNIQUE(user_id, name COLLATE NOCASE) verified: two different users may each own "Farmacologia"; the same user cannot create a case-insensitive duplicate. Migration idempotent rerun and non-destructive to pre-existing users/sessions data, both verified. Full gate: server 106/106 (was 95, +11), root 238/238, test:inventory PASS (29 files). |
| Single-save and no-partial-state | NOT_RUN | |
| Agenda + both review completion modes (T16) | PASS | server/src/services/reviews.js: `localDateString()` uses `Intl.DateTimeFormat('en-CA', {timeZone})` (not manual UTC-offset math) for local-day bucketing, so DST transitions are handled by the platform tz database rather than hand-rolled arithmetic. `agenda()` buckets overdue/today/tomorrow by `due_date` and computes `completedToday` by re-deriving the local day of `completed_at` in JS (not SQL `date()`) so the same Intl logic governs both due-bucketing and completed-bucketing; includes review-only completions (no questions) per heritage.md H-03 (Hoje is a decision surface, not a database browser). `complete()`: validates task ownership (composite user_id+id lookup, cross-user returns NOT_FOUND not 403 — no existence leak), validates questionsCount/correctCount shape only when questions are present (omitting them is a valid, non-fake review-only completion), checks idempotency BEFORE the transaction, throws ALREADY_COMPLETED if already completed (an explicit reopen is required, never a silent overwrite), and inside ONE `db.transaction()` marks completed_at and optionally inserts exactly one aggregate REVIEW learning_evidence row — server derives `score = correctCount/questionsCount`, never trusts a client-sent percentage. `reopen()` clears completed_at but never deletes/mutates prior learning_evidence rows (heritage.md H-06: history is distinct from schedule). Discovered during testing: `Date.now = () => fixedTime` does NOT reliably override bare `new Date()` construction in the current Node/V8 (confirmed via an isolated `node -e` repro showing zero effect from the mock) — this is a test-harness limitation, not a product bug; fixed by refactoring `complete()` to accept an injectable `now = () => new Date()` parameter instead of relying on global monkey-patching. server/test/reviews.test.js (11/11): localDateString timezone correctness, agenda bucket separation, completedToday includes review-only, missing questionsCount stays missing with no invented evidence, exact q/c stored once with server-derived score, correctCount>questionsCount rejected, duplicate completion without operationKey -> ALREADY_COMPLETED (not silently accepted/duplicated), idempotent retry with same operationKey+payload returns the identical cached result, reopen preserves prior evidence, a full real-HTTP integration test (register->login->me for CSRF->create a learning-unit with a 2020 study date to guarantee overdue items->GET /v1/agenda->POST complete (200, score 0.75)->second conflicting complete attempt (409 ALREADY_COMPLETED)->POST reopen (200)) exercising the actual Fastify routes/session/CSRF stack rather than calling service functions directly, and cross-user denial (NOT_FOUND for both complete and reopen on another user's task, confirmed at the service layer). Full gate: server 143/143 (was 132, +11), root 240/240, rust 13/13, build PASS, full e2e 17/17, test:inventory unaffected (no new test file outside known roots). |
| Exercises: immutable versions + provenance (T17) | PASS | server/migrations/006-exercise-archive.sql adds `exercises.archived_at` (nullable, NULL=active) via ALTER TABLE — archive-not-delete per design.md recovery/rollback guidance, following T10's precedent of ALTER TABLE for post-hoc columns rather than editing an applied migration. server/src/services/exercises.js: `create()` inserts one `exercises` row + one `exercise_versions` row in a single transaction; `edit()` NEVER updates an existing version row — it always INSERTs a new `exercise_versions` row (old ones stay byte-identical and independently queryable via `getVersion(id)`), satisfying AC-18's requirement that a past attempt scored against an earlier version keeps seeing exactly what was shown then, even after later edits. `provenance` is validated against a closed enum {MANUAL, SOURCE, AI_GENERATED} at the service layer (not a DB CHECK constraint, since `exercise_versions` was already created without one in migration 004 and no production data exists yet to force a full-table-rebuild migration — recorded honestly as an app-layer enforcement choice, consistent with the smallest-sufficient-change principle). `archive()`/`reactivate()` mirror subjects.js's soft-delete pattern; archived exercises are excluded from the default `list()` but individual exercises/versions remain resolvable by id (an archived item's version rows are never touched, so a historical attempt reference is never corrupted). `reorder()` is scoped to one unit and, like subjects.reorder, validates every id is owned before mutating anything — a foreign-owned or wrong-unit id in the list throws NOT_FOUND with zero rows changed (verified by a before/after order comparison, not just the thrown error). Ownership checks use the same composite user_id+id lookup pattern as every other T13-T16 service — cross-user access returns NOT_FOUND (no existence leak), verified directly against getById/edit/archive/list/getVersion. server/test/exercises.test.js (8/8): single create produces exactly 1 exercise + 1 version row; invalid provenance and empty question are both rejected with zero rows created; edit appends a 2nd version row (verified by exact COUNT) while the original version's content is unchanged and still resolvable by its own id; archiving leaves an existing version reference intact and excludes the exercise from the default list without deleting it (reactivate restores it); reorder is deterministic and a foreign-owned id fails the whole call with the original order provably unchanged; full cross-user denial across getById/edit/archive/list/getVersion; a full real-HTTP lifecycle test (register/login/create-unit/create-exercise/edit-exercise/archive-exercise/list-excludes-archived) through the actual Fastify routes with real session+CSRF. Full gate: server 151/151 (was 143, +8), root 240/240, rust 13/13, build PASS, full e2e 17/17. |
| Aggregate evidence + settings contracts (T18) | PASS | server/src/services/evidence.js: `create()` accepts ONLY type in {INITIAL_PRACTICE, EXTERNAL} — the check runs before any other validation, so there is no code path that can insert type='REVIEW' through this function; REVIEW rows are only ever produced as a side effect of reviews.complete() (verified directly: completing a review task inserts a row with type='REVIEW', and a same-shaped direct evidence.create() call with type='REVIEW' throws VALIDATION_FAILED and creates zero rows). Score is server-derived (`correctCount/questionsCount`) and is explicitly `null`, never `0`, when no questions were reported — "unknown performance is not zero" (design.md/tasks.md). q/c validation matches reviews.js's pattern (integer, questionsCount>0, 0<=correctCount<=questionsCount) plus a check unique to this route: correctCount without questionsCount is rejected (can't report "how many correct" without "out of how many"). `list()` supports owned unitId + evidenceDate range filters, denying a foreign unitId as NOT_FOUND. server/src/services/settings.js: `get()` returns real fixed defaults (timezone='America/Sao_Paulo', the shared REVIEW_DAY_OFFSETS) WITHOUT writing a row on a plain read — a settings row is only created the first time a user actually changes something, verified by asserting zero rows in user_settings after a pre-update get(). `updateTimezone()` is the ONLY writable field — validates the value both by an IANA `Region/City` shape check and by confirming `Intl.DateTimeFormat` actually accepts it (catches syntactically-plausible but unrecognized zones), then upserts via `ON CONFLICT(user_id) DO UPDATE`. There is deliberately no field to edit `review_schedule` — the fixed 16-offset schedule can never be changed through settings, so a timezone edit can never be read as consent to adaptive rescheduling (tasks.md T18 explicit constraint), confirmed by a test asserting reviewSchedule stays exactly REVIEW_DAY_OFFSETS after an update. Refactored server/src/services/reviews.js's private `getUserTimezone()` to delegate to `settings.get()` instead of duplicating the DB read and default constant, so agenda's timezone default now has exactly one source of truth. server/test/evidence-settings.test.js (7/7): direct-evidence type restriction + derived score + unknown-performance-is-null; q/c range/shape validation including the correctCount-without-questionsCount case, all with zero rows created on rejection; owned unit/date filters plus foreign-unitId denial; REVIEW-bypass proof (completing a review is the only path to a REVIEW row); settings defaults-without-write, validation, fixed-schedule invariant; tenant-scoped settings changes proven not to touch another user's evidence rows; a full real-HTTP lifecycle (register/login/create-unit/create-evidence/rejected-REVIEW-bypass/list/get-settings/patch-settings) through the actual Fastify routes. Full gate: server 158/158 (was 151, +7), root 240/240, rust 13/13, build PASS, full e2e 17/17. |
| Owned logical export + server backup contracts (T19) | PASS | server/src/backup.js: `createLogicalExport(db, userId)` returns a versioned (`exportVersion`, `schemaVersion` from `MAX(schema_migrations.version)`) snapshot of every row the user owns (subjects, learning_units, review_tasks, exercises, exercise_versions, learning_evidence, settings) via raw per-table SELECTs scoped by `user_id`, PLUS user.id/email_display/created_at only — never `password_hash`/`password_salt`/`password_params`, and `sessions` is never queried at all. `createPhysicalBackup(db, destPath)` uses better-sqlite3's own async `db.backup()` (safe under concurrent WAL writers, unlike copying file bytes), writing to a `${destPath}.tmp-<pid>-<ts>` path first and renaming only on success — a failed backup (e.g. destination directory doesn't exist) throws `BackupError('BACKUP_FAILED')`, removes only its own temp file, and never touches the source DB (verified: user count identical before/after a forced failure, and zero leftover `.tmp-` files). `verifyPhysicalBackup(sourceDb, backupPath)` opens the backup in a SEPARATE connection (never the live handle), runs `PRAGMA integrity_check`, and additionally compares exact row counts per table against the source — an integrity_check alone would not catch a backup that completed early with a subset of rows; a structurally-valid-but-content-mismatched file (proven with a real second empty database) is rejected as `CONTENT_MISMATCH`, not silently accepted. GET /v1/export (server/src/routes/backup.js) exposes ONLY the logical export, scoped to `request.actor.userId`; the whole-database physical backup is deliberately NOT an HTTP route (it spans every user's rows including other tenants' data and password hashes, which no per-session token can legitimately scope down to) — it is instead server/scripts/backup.mjs, an operator CLI requiring shell access, following T11's reset-password.mjs precedent for the same authorization-boundary reason. Manually verified the CLI end-to-end against a real DB file (integrity_check=ok, all 10 tables count-matched). **Real mid-task defect (code-preservation-guard incident, self-caught during full-gate rerun, not by an external reviewer):** server/src/backup.js already existed from the PR-1 foundation (commit aa62c67, T5) containing a legacy `export async function backup(db, backupDir)` (timestamped-filename physical snapshot) that server/test/persistence.test.js still depends on; creating T19's backup.js with the Write tool overwrote that file wholesale without reading it first, silently deleting the old export and breaking persistence.test.js (`SyntaxError: ... does not provide an export named 'backup'`) — caught immediately by the very next full `npm --prefix server test` gate run (1 failing file) rather than by inspection. Repaired per code-preservation-guard's protocol: restored the original `backup(db, backupDir)` function verbatim (with a comment explaining why it coexists with, rather than is merged into, T19's new `createPhysicalBackup`/`verifyPhysicalBackup` — different callers, different signatures, different guarantees) instead of deleting or rewriting persistence.test.js to match the new shape. Re-verified both `test/persistence.test.js` (2/2) and `test/backup-contract.test.js` (7/7) pass together post-repair. server/test/backup-contract.test.js (7/7): logical export round trip matches every owned row/relation exactly (including BOTH exercise_versions after an edit, proving export is not just current state) plus schema/export metadata; zero password/session fields and zero trace of another user's data in a real two-user DB; unknown-user export rejected; physical backup passes integrity_check and exact per-table content comparison against a real multi-user DB; a mismatched backup file is rejected, not accepted; a failed backup leaves the source untouched with no leftover temp file; a full real-HTTP GET /v1/export proving user-A's export contains none of user-B's data and 401s when unauthenticated. Full gate: server 165/165 (was 158, +7, includes persistence.test.js's pre-existing 2), root 240/240, rust 13/13, build PASS, full e2e 17/17, test:inventory PASS (35 files). |
| RemoteStore + DB-caller inventory (T20) | PASS | Before writing any replacement code, delegated a read-only inventory pass (a separate Explore subagent) that grepped every `DB.*` call site in src/app.js against every method defined on the old `DB` object in src/db.js (2019 lines). Produced a full contract matrix: 34 methods total, 12 confirmed dead code (schema `ensureColumns()` internals and 4 unused `learningEvidence` getters — none belong in a server-authoritative store, since the server owns its own schema), and one REAL PRE-EXISTING BUG found and NOT invented by this task: `src/app.js:1534` calls `DB.subjects.delete(id)`, a method that was NEVER defined anywhere in db.js (only `deleteIfEmpty` exists) — silently swallowed by an empty `catch{}`, meaning the "Excluir disciplina" button has been a complete no-op in production. Also surfaced two real API gaps this task had to close, not just document: `DB.learningUnits.getAll/update` and a broad (non-agenda-bucketed) `DB.reviewTasks.getAll` had no server-side equivalent at all. Added `learningUnits.list/getById/updateFields` (server/src/services/learning-units.js) — `updateFields` deliberately does NOT accept `studyDate` or `subjectId`: design.md §4's date-correction contract requires atomically reprojecting pending review offsets while preserving completed tasks/evidence, which is real scheduling surgery out of this task's scope; the AJV PATCH schema simply omits those fields so an attempt gets a clean 400, not a silent ignore (verified over real HTTP). Added `reviews.list(db, userId, {unitId})` (server/src/services/reviews.js) for the same reason — a full unscoped/unit-scoped history read distinct from agenda's date-bucketed view. New server tests (4, in test/create-unit.test.js + test/reviews.test.js): owned list/getById/updateFields with cross-user denial and the studyDate-rejection proof; list() returns all 32 tasks across two units regardless of due date/completion (proving it is NOT agenda-shaped) and is properly user-scoped even against a foreign unitId. src/api-client.js (new): a thin, typed fetch wrapper distinguishing `ApiError` (server responded, non-2xx, real `{error:{code,...}}` body) from `NetworkError` (fetch itself rejected — offline/DNS/CORS) so no caller can mistake "we don't know what happened" for "nothing happened"; CSRF header attached only for mutating methods and only once `setCsrfToken()` has been called; deliberately never sets an `Origin` header itself (a forbidden header a real browser refuses to let JS override — real browsers already send it correctly, confirmed by the untouched e2e/auth.spec.js suite still passing). src/remote-store.js (new): exports the same named `DB` shape as db.js (`import { DB } from './remote-store.js'` is a drop-in swap for every subjects/settings/exercises/learningEvidence/learningUnits.createWithReviews call site) with DTO adapters where the new domain model deliberately differs from the old local one: exercises (versioned `currentVersion` → flat questionText/answerText/hintText/position, an AC-18 fidelity upgrade hidden behind a stable read shape), learningEvidence (type→context, fractional score→percent, unknown performance stays `null` never `0`), reviewTasks (`reviewDone` synthesized from `completedAt != null`, since the new schema deliberately does not store per-task question/score fields — heritage.md explicitly rejects "manual checkboxes for derivable facts"; per-task score now lives in learningEvidence joined by reviewTaskId, a screen-adaptation item left honestly open for T21/T22, not papered over here). `subjects.delete` now routes to the real `deleteIfEmpty` endpoint — fixing the dead call to a real, safely-guarded operation (409 SUBJECT_NOT_EMPTY on a non-empty subject) instead of a silent no-op. `reviewTasks.update(id, fields)` maps the two coherent legacy partial-update shapes found in the inventory (`{reviewDone}` and `{questionsCount,correctCount}`) onto `complete()`/`reopen()`, and explicitly THROWS `RemoteStoreError('UNSUPPORTED_PARTIAL_UPDATE')` for the third shape (`{questionsDone}` alone) rather than silently no-op'ing it — there is no honest atomic equivalent for "questions attempted, score unknown" in the new model. `importAll`/`clearAll` throw `RemoteStoreError('NOT_YET_SUPPORTED')` immediately (verified: zero network calls) rather than a silent no-op — consistent with tasks.md T22's explicit instruction to "offer legacy export until migration completes" and "replace dangerous local reset affordances... with an explicit supported owned operation," which is T22's UX decision to make, not T20's to fake. test/remote-store.test.js (17/17): a structural contract-matrix test asserting every caller-inventoried method still resolves to a callable function; mocked-transport tests proving exact request shaping (credentials always included, Content-Type only with a body, CSRF only on mutating methods) and both `ApiError`/`NetworkError` mapping; DTO-adapter tests for exercises/evidence/reviewTasks; proof that all four "reject locally" cases (`learningUnits.update` with studyDate/subjectId, `importAll`, `clearAll`, `learningEvidence.create` with type REVIEW, `settings.update` with a non-timezone field) throw BEFORE touching `fetch` at all; and two **real, non-mocked** integration tests against an actually-listening server instance (real HTTP, not `app.inject()`) covering the full authority chain — session cookie, CSRF token, and per-user ownership — across subjects/learning-units/agenda/exercises/evidence/settings/export, plus a second-account isolation proof and a real 401 for an unauthenticated request. (Node's built-in fetch has no automatic per-origin cookie jar or auto-Origin-header the way a real browser does — a harness-only shim in the test file supplies both for these two real-API tests; api-client.js itself sets neither, since a real browser already provides both correctly per the untouched, still-passing e2e/auth.spec.js suite.) No generic SQL/owner-injection route exists anywhere in this layer — every request rides the session cookie, and the server derives user_id itself; RemoteStore has no method that accepts or forwards an owner id. Full gate: root 257/257 (was 240, +17), server 169/169 (was 165, +4), rust 13/13, build PASS, full e2e 17/17 (unchanged — old db.js/BrowserStore path untouched, actual cutover is T21), test:inventory PASS (36 files). |
| Connect Web to real server reads/writes (T21) | PASS | **Configuration-controlled authority switch, not a wholesale replacement** (recovery/rollback requirement): src/app.js now imports both `LocalDB` (db.js) and `RemoteDB` (remote-store.js); a runtime flag `window.__SMARTLEARN_REMOTE_MODE__` (default unset/false — read the same way `window.__SMARTLEARN_API_BASE__` already was for T11's auth-ui.js) selects which one `DB` resolves to. index.html sets the default from `import.meta.env.VITE_REMOTE_MODE` only if the flag isn't already set (so a test's `page.addInitScript` override always wins). This means EVERY existing screen/test that never touches the flag (all 17 pre-existing e2e tests, the full manual click-through I did in-browser) is completely unaffected — db.js is untouched and remains the shipped default; remote-store.js is the opt-in, explicitly-staged path, exactly matching "never silently select a different authority." One origin: vite.config.js proxies `/v1` and `/health` to the API server in dev (`SMARTLEARN_API_PROXY_TARGET`, default `http://localhost:3000`); server/src/app.js gained an opt-in `staticDir` option (guarded — every existing call site omits it and is completely unaffected, verified by a dedicated test) that serves the built SPA via the newly-added `@fastify/static` (installed at the patched 10.1.3 — the initial `npm install` pulled a vulnerable ≤10.1.1 range, caught by `npm audit` before use and upgraded, 0 vulnerabilities) with an SPA-fallback 404 handler for client-side routing, while `/v1`/`/health` remain strictly JSON (verified: an unknown `/v1` path never falls back to SPA HTML). Authenticated bootstrap: src/auth-ui.js's `bootstrap()`/`logout()` now also call `setCsrfToken()` from the new src/api-client.js so the CSRF token has exactly one source of truth shared between the auth/* routes and every domain route. Login-gate: showScreen() redirects any data screen (Hoje/Plano/Estatísticas/Acompanhar/Disciplinas/Configurações) to Conta when in remote mode with no authenticated session — this is the literal "distinguish unavailable server from empty data" requirement: a reachable-but-unauthenticated server is a visible login prompt, never rendered as an empty agenda. **Two real product bugs found and fixed while actually using the app in a real browser (not just unit tests)**, both caught before any e2e was written: (1) POST/PATCH `/v1/learning-units`'s AJV schema declared `summaryBody`/`sourceText` as `type:'string'` only — the existing UI has always sent `null` for an empty optional field (`value.trim() || null`, unchanged product behavior since before this task), which AJV strict mode rejected with a 400, breaking the single most central write in the app (creating a unit) the moment it was driven end-to-end; fixed by widening those two fields (and the analogous `answer`/`hint` on exercises, same pattern) to `type:['string','null']`. (2) The old code path that ran unconditionally on load right after `await dbInit` (`renderSubjects()+renderToday()`, bypassing showScreen's own gate) would have 401'd before a session existed; gated on `authenticated` too. Both were caught by manually registering, logging in, and creating a real unit against a real locally-spawned server in the actual Browser pane before writing the automated E2E — exactly the "start the dev server and use the feature in a browser" verification this kind of change requires. Also fixed a cosmetic-but-real gap: the mapped reviewTask DTO's `reviewNumber` was undefined (the new schema has no such column), rendering a literal "R{undefined}" badge; extended the server's taskDto with `offsetDays` and derived `reviewNumber` client-side from its position in the shared `REVIEW_DAY_OFFSETS` — real correct numbering, not a fallback placeholder. server/test/static-serving.test.js (4/4): staticDir opt-in has zero effect on any call site that omits it; a nonexistent staticDir is rejected loudly (not silently serving nothing); SPA root/asset/deep-link-fallback all serve correctly from a real (fixture) built directory; `/v1` and `/health` never fall through to SPA HTML even on an unmatched path. src/remote-store.js/api-client.js test additions (2 tests strengthened): reviewNumber derivation verified against two different real offsets. e2e/server-authority.spec.js (5/5, NEW, real server child-process + real browser + REMOTE_MODE actually flipped on — the one file that exercises the new authority end-to-end): unauthenticated visitor redirected to login instead of an empty agenda; **two independent browser contexts (separate cookie jars) logged in as the SAME account observe the exact same state — one context completes a review, the other sees "1 feita" after its own refresh, proving the server, not either browser, is the authority**; a second, different account sees zero trace of the first account's data; data created before a server restart (kill + respawn the real child process) is still there after it comes back up; a network-loss write (`context.setOffline(true)`) produces a visible "Não foi possível salvar" error with the draft preserved, never a silent local success, and confirmed absent after reload once back online. Full gate: server 173/173 (was 169, +4), root 257/257 (was 240, +17 from T20 remote-store.test.js, unchanged this task), rust 13/13, build PASS, full e2e 22/22 (17 existing unchanged + 5 new), test:inventory PASS (38 files). Independent Fresh Verifier for this Phase 03 slice is explicitly assigned to T24 ("Close the server-authoritative Web slice") per tasks.md, not duplicated here. Known, deliberately-scoped-out gaps for T22 to close: the per-task detail panel (questions-done checkbox, score inputs, comment field, external-exercise registration) still reads/writes fields the new schema doesn't have — each of those specific controls fails loudly with an existing, already-tested error message (e.g. "Não foi possível salvar o comentário") rather than crashing or silently succeeding, since RemoteStore's `reviewTasks.update` explicitly rejects those unmappable shapes (T20); Disciplinas/Estatísticas/Acompanhar/Configurações screens work for their basic list reads but haven't been driven through dedicated remote-mode E2E yet; import/reset remain explicitly NOT_YET_SUPPORTED. |
| Complete screen and settings parity (T22) | PASS | **Three real DTO/signature-mismatch bugs found and fixed, all caught by actually driving the app in a real browser against a real spawned server (not by reading code or trusting unit tests alone)**, none caught by the T20/T21 unit/e2e suites at the time because those suites happened to only exercise the subset of calls that were already correct: (1) `src/remote-store.js`'s `exercises.create` took one merged `{unitId,...}` object; `src/db.js`'s real signature (and every real caller, e.g. the "add-exercise" handler) is `create(unitId, fields)` — two positional args. Fixed to match exactly. (2) `subjects.create` had the identical class of bug — remote-store expected `{name,color}`, but `src/db.js`/the Cadastro screen's "Nova disciplina" form call it as `create(name, color)`, two positional args with `color` defaulting to `'DISC-BLUE'`. Fixed identically. Both were manually reproduced first (a real "Não foi possível adicionar a disciplina."/broken exercise flow against a live server), then fixed, then confirmed working live in the browser, THEN backed with new mocked-transport tests in test/remote-store.test.js (2 new) asserting the exact request body shape sent for each. (3) server/src/services/exercises.js's `edit()` required a full `{question,provenance}` resubmission on every edit, but the real "save-exercise-edit" UI handler only ever sends `questionText/answerText/hintText` — never provenance — which would have 400'd on every single edit in remote mode; fixed `edit()` to be a genuine partial update (merges any omitted field onto the current version's value), matching how every other update endpoint in this codebase already behaves; confirmed live (edited only the hint field of a real exercise against a real server, provenance/question/answer all correctly carried over unchanged) and covered by a new server-side test. A fourth, non-signature bug was also found live: `.secondary-button`/`.primary-button` set their own `display` (an author rule that always beats the browser's default `[hidden]` style regardless of source order), so `element.hidden = true` had silently NO visual effect on any button in this codebase — discovered because the newly-hidden "Importar backup"/"Apagar banco todo" buttons (see below) stayed visible despite the DOM attribute being correctly set; fixed with a scoped `.settings-actions [hidden] { display: none; }` CSS rule (not a project-wide `[hidden]` override, to avoid touching unrelated existing `[hidden]` behavior elsewhere in the stylesheet). **A wrong-then-corrected product decision, recorded honestly per TLC discrimination discipline**: initially treated `#register`/"Cadastro" (nav-hidden since a prior P1-2 decision) as a redundant duplicate of Plano's create-unit flow and redirected it away — this was WRONG, caught by live verification: Cadastro is the ONLY UI in the app that manages exercises (add/edit/delete); Plano's own unit rows have no exercise-authoring section at all. The redirect was reverted before landing (`#register` remains reachable exactly as P1-2 left it — nav-hidden, but a direct/typed/bookmarked hash still works), and this reversal is itself the "preserve required entry behavior" decision tasks.md T22 asks for, not the redirect. A second real, independent, previously-silent gap was found in the same investigation: Cadastro's own study list (`renderStudies()`) was never wired to run on plain navigation to `#register` (only after a NEW submission through that same form), so a unit created elsewhere (e.g. via Plano) was invisible there and its exercises consequently unmanageable — fixed by adding the missing `if (nextScreen === "register" && databaseAvailable) renderStudies()` dispatch line, matching the pattern every other screen already uses. Screen-by-screen remote-mode status, each confirmed either by direct code-path analysis (fields the screen actually reads/writes cross-checked against remote-store's real DTOs) or live browser + new E2E: Hoje/Plano — full parity (T21). Estatísticas (`Stats.calculate`) and Acompanhamento (`tracking-state.js`) — both ALREADY fully compatible with zero code changes needed, since their entire reviewTask field dependency is exactly `{reviewDone, dueDate}`, both of which remote-store's DTO already provides correctly; confirmed live (real server, real completed review, correct reviewsDone count and non-empty tracking list). Disciplinas — full CRUD (create/rename/archive/reactivate/delete) already matched remote-store's DTOs exactly and needed no changes; confirmed live end-to-end including the historically-broken "Excluir" button (T20's fix). Cadastro/exercises — fixed as above, confirmed live. Configurações — "Exportar backup" already worked via `/v1/export`; found and fixed one more real bug in the same investigation: `exportBackup()`'s post-success `DB.settings.update({lastBackupAt})` bookkeeping call would throw in remote mode (settings only accepts `timezone`) and, being in the same try block, would have reported an already-successful, already-saved export as a failure — wrapped in its own try/catch so a bookkeeping failure can never overturn a real success. "Importar backup"/"Apagar banco todo" (both explicitly NOT_YET_SUPPORTED server-side per T20) now render as a genuine "safe explanatory state" (hidden buttons + a plain-language reason) instead of a permanently-failing danger button, per tasks.md T22's explicit instruction — confirmed live and via e2e. Hoje's per-task detail panel (questions-done checkbox, free-form score inputs, comment field) has no server-model equivalent by design (heritage.md's rejection of "manual checkboxes for derivable facts") and is now cleanly omitted in remote mode rather than rendered broken — local mode is completely unaffected (byte-identical behavior, confirmed by all 12 smartlearn-plan-flow.spec.js tests still passing unchanged). New tests: test/remote-store.test.js +2 (subjects.create/exercises.create exact-signature assertions), server/test/exercises.test.js +1 (edit() partial-update semantics), e2e/feature-parity.spec.js (new, 4/4: Disciplinas full CRUD; Estatísticas+Acompanhamento render correctly after a real completed review; Cadastro/exercises full CRUD; Configurações safe-explanatory-state). Full gate: server 174/174, root 259/259 (+2), rust 13/13, build PASS, full e2e 26/26 (22 unchanged + 4 new), test:inventory PASS (39 files). |
| Prove one-intention UX, retries and recoverable errors on server (T23) | PASS | Ports the one-save acceptance (T03/AC-03/AC-04) from BrowserStore to real server transactions. Client wiring: `src/app.js` gained a small per-form `createOperationKeyTracker()` (one instance each for Plano's `planUnitSaveBtn` handler and Cadastro's `studyForm` submit handler — two independent user intents, never sharing a key) that generates a `crypto.randomUUID()` operation key kept stable across a failed attempt and renewed only on success, explicit cancel, or a definitive (non-`NetworkError`) rejection; a `NetworkError` (fetch itself failed — we genuinely don't know whether the server committed) deliberately keeps the same key so a retry of the same draft replays as an idempotent no-op instead of a second unit. No server change was needed for this wiring: `remote-store.js`'s `createWithReviews(data)` already forwards the whole `data` object (including `operationKey`) straight through to `POST /v1/learning-units`, and T15's `server/src/services/idempotency.js` already persists/replays by `(userId, operation, operationKey)` — confirmed by reading both files before writing any new code, not assumed. New `e2e/atomic-save.spec.js` (4/4, real spawned server child process + real browser, REMOTE_MODE on, no debug endpoints or in-process mocks of product code) proves all four required fault scenarios against the real HTTP surface: (1) **duplicate intent (double-click)** — two concurrent, byte-identical `fetch()` calls made directly from the page under the same operation key (the real fault a double-click that escapes the button's own `disabled` race would produce) both resolve to the exact same `unit.id`, and the server ends up with exactly 1 unit / 16 review_tasks, not 2/32; (2) **response-lost-after-commit** — `page.route()` lets the POST reach the real server via `route.fetch()` (so it genuinely commits) then `route.abort()`s the response before the page sees it, which `api-client.js` correctly classifies as `NetworkError`; the UI shows a clear failure with the draft intact, and a same-key retry after removing the interception replays the original result (still 1 unit / 16 reviews) rather than creating a second one; (3) **mid-transaction failure** — a real, pre-existing UNIQUE(user_id, name COLLATE NOCASE) constraint (`server/migrations/004-learning-domain.sql`) is hit via `resolveOrCreateSubject`'s own duplicate-name check, which runs INSIDE the same `db.transaction()` as the would-be unit+16-review insert (`server/src/services/learning-units.js`); submitting a second unit under an already-used subject name aborts the transaction and leaves zero orphan `learning_units`/`review_tasks` rows and no duplicate subject — a genuine production business rule firing mid-transaction, not a test-only fault hook, satisfying the task's "inject faults in test harness, not production debug endpoints" constraint; (4) **render-failure-after-successful-save** — the POST is left untouched (must succeed) while the follow-up GET the UI uses to refresh Plano is aborted; the existing `app.js` inner try/catch (pre-existing code, confirmed by reading it before writing the test) correctly falls back to "Aula salva. Recarregue para ver o resultado." with the draft already cleared (nothing left to accidentally resubmit), and reloading afterward shows exactly 1 unit / 16 reviews, proving the render failure never causes a silent duplicate. Full gate: server 174/174 (unchanged), root 259/259 (unchanged), rust 13/13 (unchanged), build PASS, full e2e 30/30 (26 unchanged + 4 new), test:inventory PASS (40 files). |
| Close the server-authoritative Web slice (T24) | PARTIAL — gate gap, NOT closed | Independent Fresh Verifier (separate subagent from the T20-T23 maker, per this task's own governance requirement) ran against frozen HEAD `9a8c7cb`: fresh reruns of every gate (server 174/174, root 259/259, rust 13/13, build PASS, full e2e 30/30, test:inventory PASS 40 files) plus 32/32 independent standalone HTTP checks against a real spawned server + real SQLite DB (2 real accounts, raw `fetch()` + hand-rolled cookie jar) proving cross-account isolation across every domain object (subjects, units, review tasks, evidence, settings, exercises, export) — list endpoints never leak another account's rows, direct-ID GET/PATCH/DELETE against another account's row returns 404 not a silent no-op, unauthenticated/no-CSRF requests are rejected — plus a field-by-field static comparison of every `src/remote-store.js` DTO mapping against its actual server route/service (subjects, learning-units, review-tasks/agenda, learning-evidence, settings, exercises, export), and a structural trace confirming no writable local (BrowserStore) fallback exists in remote mode (`src/app.js`'s `DB` alias is a single `const`, assigned once). VERIFIER_RESULT=PASS on everything it checked; zero product defects found; no code fix made or needed. However, checked against T24's own literal done-when criteria (tasks.md), this does NOT fully close T24: criterion 3 reads "Gate passes: Full relevant code gates, production-build E2E and independent verifier at frozen candidate" — production-build E2E is a distinct, separate requirement from the dev-server e2e suite and from the independent verifier. `playwright.config.js`'s only `webServer` config is `npx vite --port 5199 --strictPort` (the Vite DEV server) — confirmed by reading the file directly, not assumed — and no Playwright project/config anywhere in this feature's history (T20 through T23, nor this verifier pass) has ever run the e2e suite against a production build (`npm run build` + `npm run preview`, or the server's own T21 `staticDir` SPA-serving mode). Recorded here honestly rather than marking T24 done on a partial match: T24 stays OPEN, tasks.md's 3 done-when boxes remain `[ ]` (boxes 1-2 are arguably satisfiable by the evidence above; box 3 is not, specifically on the production-build clause). Phase 03's exit gate is therefore NOT satisfied. Concrete remaining work for T24: either add a production-build E2E gate (second Playwright project/config pointed at a real `npm run build` + `vite preview`/`staticDir`) and record its result, or make an explicit, recorded scope decision that dev-server E2E is accepted in place of production-build E2E for this task — before checking T24's boxes. |
| T24 gap closure — production-build E2E | PASS — T24 now fully closed | Closed the one remaining gap from the row above with the smallest sufficient harness: new `e2e/production-build.spec.js` builds the real SPA fresh (`npm run build` -> `dist/`), spawns the real `server/src/main.js` with `SMARTLEARN_STATIC_DIR` pointed at that `dist/` (T21's own single-process production-serving path — `@fastify/static` + SPA fallback, exactly as it ships), and drives a real browser directly at that server's own origin (no Vite, no dev server, no proxy — genuinely one origin for both the built SPA and the `/v1` API). One deliberately minimal walking-skeleton journey: asserts the served HTML contains no `@vite/client` (proving it's really the built artifact, not an accidental dev-mode response) and the page title is correct, then register -> login -> create a subject+unit (16 reviews) via the real Plano UI -> reload -> unit still there. The identity/ownership/parity/no-local-fallback properties this task's other acceptance criteria cover were already independently proven at the HTTP/DTO level in the row above; this test's only job was proving the packaged build artifact itself isn't broken, so it was kept to one test rather than re-deriving already-proven coverage. Two real harness bugs found and fixed while building this (both in the new test file, not product code): (1) first attempt only set `window.__SMARTLEARN_REMOTE_MODE__` in `page.addInitScript`, omitting `window.__SMARTLEARN_API_BASE__` — `src/auth-ui.js`'s `API_BASE` falls back to a hardcoded `http://localhost:3000` when that override is absent, so every auth call silently went to the wrong port; fixed by setting both, matching the existing pattern in `e2e/server-authority.spec.js`. (2) clicking the Plano nav immediately after the login form's submit click raced the async `renderAccount()` inside `src/app.js`'s login handler (which is what actually flips `authenticated = true`) — sometimes the click landed before that promise settled and the auth-gate bounced back to Conta; fixed by waiting on `#account-logged-in-view` to become visible first, the same explicit signal `e2e/server-authority.spec.js` already uses for the identical race, rather than an arbitrary sleep. Confirmed not flaky (3 consecutive clean runs of the new spec alone). Full e2e suite re-run with the new spec included: 31/31 PASS (30 existing unchanged + 1 new), test:inventory PASS (41 files, was 40). Server/root/rust suites were not re-run — nothing outside `e2e/` and this documentation changed, so re-running them would not have exercised anything different from their last recorded PASS (server 174/174, root 259/259, rust 13/13, both already current at this HEAD). T24's 3 done-when boxes are now genuinely all satisfied: independent verifier PASS (row above) + this production-build E2E PASS + full relevant code gates green. Phase 03's exit gate is now satisfied; Phase 03 is closed. |
| Normalize supported legacy snapshots without data invention (T25) | PASS | Built `shared/import-normalization.js` (`normalizeLegacyExport(raw)`) — pure, read-only, no I/O — against the REAL v1/v2/v3 legacy shapes `src/db.js` already produces/consumes (`migrateV1ImportData`, `buildImportStatements`, `exportAll`), not invented shapes: golden fixtures in `server/test/import-fixtures/` were built directly from the literal v1 (`studyRecords`/`sources`) and v2 (`schemaVersion:2`, no top-level `learningEvidence`) test fixtures already committed in `test/learning-evidence.test.js`/`test/learning-units.test.js`, plus a fresh v3 fixture matching `exportAll()`'s real shape. Detects source version (v1 via `studyRecords` presence with no `schemaVersion`; v2/v3 via explicit `schemaVersion`; anything outside `[1,3]` rejected as `UNSUPPORTED_VERSION`) and maps every entity (subjects, learningUnits, reviewTasks, exercises) to a canonical `legacy*Id`-keyed shape — final ID assignment is explicitly deferred to T26 per design.md §6, not decided here. Per design.md §6's explicit hard-reject list, the WHOLE import is rejected (never a partial success) on: unknown/unsupported version, duplicate ids within one entity type, invalid/impossible calendar dates (round-trip-checked, e.g. 2026-02-30), a learningUnit/reviewTask/exercise/evidence row referencing a nonexistent parent id (cross-entity dangling reference), invalid or out-of-range questionsCount/correctCount, a missing/unrecognized exercise `provenance` (cannot be guessed without misrepresenting origin — this is the concrete "unsupported field that would lose meaning" design.md names), and — the acceptance criterion's own named discriminating case — a completed+scored reviewTask (v3) with no matching explicit `learningEvidence` row, caught as `MISSING_EVIDENCE_ROW` rather than letting the score silently vanish. All issues found in one pass are collected and thrown together (`ImportNormalizationError.issues`, verified by a dedicated test injecting two independent problems at once), not just the first — so a future real preview (T26) can show a user everything wrong with a source at once. Purely cosmetic gaps (missing subject color, missing sortOrder, missing unit createdAt/updatedAt) are filled with the same safe defaults `src/db.js`'s own production import path already uses and recorded in a `warnings` array, never silently dropped and never blocking. v1/v2 sources with no first-class evidence array synthesize REVIEW evidence only from reviewTasks that are genuinely `reviewDone && questionsDone` with a real `questionsCount` — mirrors `db.js`'s own v2-upgrade logic (same field derivation) so both import paths agree; an unfinished review task never becomes a fabricated evidence row. `server/test/import-normalization.test.js` (14/14): each of v1/v2/v3 golden fixtures normalizes correctly (including the exact expected evidence-synthesis output for v2, verified field-by-field via `deepEqual`), determinism (same input run twice -> `deepEqual` identical output), warnings recorded for cosmetic defaults, and one dedicated rejection test per hard-reject class above (7 negative fixtures) plus the multi-issue-reported-together test and a malformed-top-level-input test. Full gate: server 188/188 (was 174, +14), root 259/259 (unchanged — `shared/import-normalization.js` is server-only for now, no client import site), build PASS, test:inventory PASS (42 files, was 41). Rust suite not re-run — this task touched no `src-tauri` code, so its last recorded 13/13 stands. No migration UI, preview, or commit-to-account logic exists yet — this task is deliberately read-only normalization only, exactly as scoped; T26 (preview/ID-mapping) and T27 (atomic commit) are separate, not-yet-started tasks. |
| Implement import preview and explicit ID mapping (T26) | PASS | New `server/migrations/007-import-previews.sql` (`import_previews` table, `UNIQUE(user_id, source_checksum)`) + `server/src/services/imports.js` + `server/src/routes/imports.js` (`POST /v1/imports/preview`, `GET /v1/imports/:id`), wired into `server/src/app.js`. `createPreview()` runs T25's `normalizeLegacyExport()` first (an unsupported/unknown source is rejected with 400 `INVALID_SOURCE` before any row is written — verified by asserting zero `import_previews` rows exist afterward), computes a SHA-256 checksum of the exact re-stringified payload received (never a client-supplied hash), and persists a report (`counts`, `warnings` from T25, `conflicts`, proposed `mapping`) scoped to `request.actor.userId`, expiring in 30 minutes (same convention as T11's reset tokens). Per design.md §6 "nonempty conflicts never overwrite by name": the only independent uniqueness constraint in this schema is `subjects(user_id, name)` (T13), so a legacy subject whose name collides (case-insensitive, via the same shared `nameKey()` subjects.js itself uses) with an existing ACTIVE owned subject is reported as a `NAME_ALREADY_EXISTS` conflict and mapped `CONFLICT` rather than `CREATE` — verified with a real prior `POST /v1/subjects` before previewing a source with the same name. Every other entity (learningUnits/reviewTasks/exercises) has no naming constraint of its own, so each is proposed `CREATE` under its own `legacySubjectId`/`legacyUnitId` — a real numeric new-row id cannot exist before an actual commit transaction runs (T27, not built), so "proposed mapping" is explicitly a CREATE/CONFLICT plan keyed by `legacyId`, not a fabricated final id; this scoping decision is recorded here since design.md doesn't spell out the exact mapping shape. Resubmitting the byte-identical source by the same owner renews the existing row (`UNIQUE` constraint) instead of accumulating a duplicate — verified: same `preview.id` returned twice, exactly 1 row in `import_previews`. Cross-user reuse fails exactly like `subjects.js`'s existing foreign-id pattern: `GET /v1/imports/:id` scopes by `id AND user_id`, so a different account's request returns 404, never confirming the preview's existence. Stale (expired) and tampered previews are rejected: `getPreview()`/`verifyPreview()` take an injectable `now` (same pattern `reviews.js`'s `complete()` already uses) and were tested directly at the service level with a fixed clock — a preview read after its 30-minute window throws `PREVIEW_EXPIRED`; `verifyPreview()` (the gate a future commit step must pass, exercised directly since no commit route exists yet) re-hashes a claimed resubmission and throws `PREVIEW_TAMPERED` when a single field differs from the original bytes, while the untampered original still verifies at the same instant. `server/test/import-preview.test.js` (7/7): zero-side-effect (all 5 domain tables stay empty after a preview), conflict reporting, cross-user 404, unsupported-schema rejection with zero preview rows created, dedup-by-checksum renewal, expiry, and tamper detection. Full gate: server 195/195 (was 188, +7), root 259/259 (unchanged), build PASS, test:inventory PASS (43 files, was 42). Rust not re-run — untouched by this task, last recorded 13/13 stands. No commit-to-account logic exists yet (T27, not started) — a preview can never itself mutate a subject/learningUnit/reviewTask/exercise/learningEvidence row, by construction (the service module contains no INSERT/UPDATE/DELETE against those tables at all). |
| Commit imports atomically and idempotently (T27) | PASS | Extended `server/src/services/imports.js` with `commitImport(db, userId, previewId, now)` + `server/migrations/008-import-commit.sql` (`import_previews.committed_at`/`commit_result_json`) + `POST /v1/imports/:id/commit`. Owned/unexpired guard reuses the same lookup T26 already proved (404/410). A previewId is already 1:1 bound to one exact checksum-verified source (T26), so re-committing the SAME previewId — by the same owner, any time, no operation key needed — returns the ORIGINAL stored result and inserts nothing new; this is a deliberate design choice recorded here since design.md doesn't mandate the exact idempotency mechanism, only that one exists ("record source checksum/idempotency"), and binding idempotency to the checksum-bound preview identity itself is strictly stronger than an optional client-supplied `operationKey` (T15's pattern elsewhere) — no retry can accidentally skip it. A nonempty conflict list (T26's `NAME_ALREADY_EXISTS`) refuses the whole commit with 409 `IMPORT_HAS_CONFLICTS` — resolving/remapping a conflict is explicitly out of this task's scope (T28), so nothing is ever silently overwritten or skipped by name. Capacity preflight (`config.importMaxRows`/`config.importMaxBytes`, env-overridable, defaults 5000 rows / 5MB) runs against the preview's already-computed counts and stored `normalized_json` byte length BEFORE opening any transaction — a batch over the limit gets 413 `IMPORT_TOO_LARGE` with zero rows touched. The actual apply is one `db.transaction()`: subjects -> learningUnits -> reviewTasks (offset_days recomputed as UTC calendar-day arithmetic between the unit's studyDate and the task's dueDate, matching `shared/review-schedule.js`'s own arithmetic, since T25's normalized reviewTask doesn't carry an offset field) -> exercises+exercise_versions -> learningEvidence, each step resolving its `legacy*Id` references through this commit's own freshly-built id maps; a reference that cannot be resolved throws `IMPORT_INTEGRITY_ERROR` inside the transaction, which better-sqlite3 rolls back atomically (proven per entity-boundary: corrupted subject/unit/reviewTask references at the learningUnits, reviewTasks, exercises and learningEvidence insertion points each independently roll back ALL 6 tables to zero rows for that user). A final reconciliation step compares each entity's actually-inserted count against the preview's own reported `counts` before the transaction is allowed to return — a stored count no longer matching the real normalized data (simulating a corrupted/edited preview row) is caught here too, not silently committed. `server/test/import-commit.test.js` (11/11): happy-path commit matches previewed counts exactly (verified against real inserted rows, including exact `offset_days`); duplicate commit of the same previewId adds zero records and returns the identical result; a foreign-owned previewId 404s with zero rows created; an unresolved conflict refuses the whole commit; an oversized batch (`config.importMaxRows` temporarily lowered) is rejected before any write; four independent corrupted-reference rollback tests (subject/unit/unit/reviewTask boundaries); one reconciliation-mismatch test; one expired-preview rejection test. Full gate: server 206/206 (was 195, +11), root 259/259 (unchanged), build PASS, test:inventory PASS (44 files, was 43). Rust and e2e not re-run — untouched by this task (server-only change, no `src-tauri` or `e2e/` files touched), last recorded values stand (rust 13/13, e2e 31/31). No migration UI exists yet — T28 (upload/preview/confirm/results UI + recovery rehearsal) is a separate, not-yet-started task; this task's commit route exists and is tested directly (service + real HTTP), but nothing in the product UI calls it yet. |
| Deliver migration UI and recovery rehearsal (T28) | PASS | `src/migration-ui.js` (previewImport/getImportPreview/commitImportPreview/buildMigrationReport — pure, DOM-free, mirrors `src/auth-ui.js`'s separation of concerns) wired into a new "Importar de um backup antigo" card on Configurações (`index.html`, `src/app.js`), REMOTE_MODE-only (no local-only equivalent exists). Flow: select file -> `POST /v1/imports/preview` renders counts/warnings/conflicts, confirm disabled while any conflict is unresolved -> confirm (behind a real confirmation dialog, since this writes real/definitive data) -> `POST /v1/imports/:id/commit` -> result panel with a downloadable JSON report (`buildMigrationReport`, saved via the same `saveBackupFile` platform-aware save path — native dialog on Tauri/Android, `<a download>` in browser — already used by the existing backup-export flow). `e2e/migration.spec.js` (3/3): (1) a fresh test user migrates the real `server/test/import-fixtures/v3-schema.json` fixture end-to-end through the actual UI, then proves a full owned export round trip — `fetch(API_BASE + '/v1/export', {credentials:'include'})` over the real HTTP session against the real spawned server, not a mock — confirming server-side persistence, not just client-side rendering of the commit response, plus a byte-identical check that the source file on disk was never touched; (2) recovery rehearsal: a cancelled preview changes zero owned rows and the same account can still complete a real migration afterward; (3) recovery rehearsal: re-uploading the same fixture into an already-migrated account surfaces the name conflict and blocks confirm, committing nothing extra. `.specs/features/smartlearn-v1-consolidated-v2/migration-runbook.md` records the pipeline (T25-T28) end to end and, per this task's own scoping instruction ("never count a fixture rehearsal as the real-user cutover"), an explicit `HUMAN_GATE: REAL_USER_MIGRATION_APPROVAL` section stating "Live approval status: NOT GRANTED" with a pre-flight checklist for whenever a real user's own export is actually migrated — every e2e run is a synthetic test account against the fixed fixture, never real user data. Independent data-integrity review (this task's own required gate, a fresh subagent with no context from the build, matching this project's established T24 fresh-verifier pattern) ran in two rounds: round 1 found 2 real bugs — (a) `commitImport`'s expiry check ran before its `committed_at` cached-result short-circuit, so retrying an already-succeeded commit after the original 30-minute preview window elapsed threw `PREVIEW_EXPIRED` instead of returning the original result, breaking AC-12's idempotent-retry guarantee for the concrete case of a lost response after a real success; (b) preview-time conflict detection scoped its existing-subject query to `is_active = 1` only, so a name colliding with an ARCHIVED subject was invisible at preview time (reported CREATE) and only surfaced as an unclassified 500 at commit time when the real `UNIQUE(user_id, name)` constraint fired — both are project-invariant-safe (NO_DATA_LOSS held in both cases, rollback was clean) but real contract breaks, not just observations. Both fixed in `server/src/services/imports.js` (reordered the cached-result check; dropped the `is_active` filter and added an `ARCHIVED_HOMONYM` vs `NAME_ALREADY_EXISTS` reason, mirroring `services/subjects.js`'s own archived-homonym distinction), with new discriminating tests in `server/test/import-commit.test.js` and `server/test/import-preview.test.js`. Round 2 (a second, independent fresh subagent, not the one that made the fixes) confirmed both fixes correct and complete, reasoned through additional edge cases (double-active-and-archived-match impossibility given the UNIQUE constraint; other call sites' own `is_active` scoping being intentionally different, not inconsistent; corrupt-commit-result being a pre-existing theoretical DB-corruption scenario unrelated to the reordering) and found zero remaining defects: `VERIFIER_RESULT=PASS`. Full gate after both fixes: server 208/208 (was 195 pre-T27, +11 T27 +2 review-fix tests), root 259/259 (unchanged), build PASS, test:inventory PASS (45 files, was 43 pre-T27, +1 import-commit.test.js +1 migration.spec.js), full `npx playwright test` suite 34/34 (31 pre-existing + 3 new, confirmed no regressions). Rust not re-run — untouched by T28, last recorded 13/13 stands. This closes Phase 04 (T25-T28 all proven) — Phase 05 (Learning Evidence, T29-T33) is the next phase, not started. |
| Add attributable item-level event schema (T29) | PASS | `server/migrations/009-learning-events.sql` (plan's suggested "004-learning-events.sql" renumbered to 009 — 004-008 already committed for T13/T15/T17/T26/T27, migrations are append-only, same renumbering precedent T13 itself recorded) adds `competencies` (owned label, `UNIQUE(user_id, unit_id, label COLLATE NOCASE)`), `exercise_attempts` (owner/unit/optional-competency/exercise_version, status, max_assistance) and `learning_events` (immutable ledger: owner/unit/optional-competency/attempt/exercise_version, kind, outcome, assistance_available/used, assessment_method, provenance, schema_version, occurred_at/recorded_at). All child FKs are the established composite `(user_id, parent_id)` against a `UNIQUE(user_id, id)` parent (design.md §3 pattern from T13/T15), so cross-user/cross-unit linkage is a database-level impossibility. `UNIQUE(user_id, attempt_id, sequence)` on `learning_events` is the literal "attempt/sequence idempotency" design.md §"Reconstructed learning evidence" calls for — a replayed/duplicate submit for the same attempt+sequence is rejected by the database itself (MX11), not merely de-duplicated in application code. A `CHECK` enforces kind/corrects_event_id as a strict two-way pair: `(kind='CORRECTION' AND corrects_event_id IS NOT NULL) OR (kind!='CORRECTION' AND corrects_event_id IS NULL)` — an ATTEMPT event can never silently carry a stray correction link, and a CORRECTION can never exist unlinked; verified the original event a correction targets stays byte-identical (queried after insert, not merely asserted by the schema). Absence maps to UNKNOWN, never a false-certainty default (MX10, the concrete fault this schema exists to make structurally impossible): `learning_events.assistance_available`/`assistance_used` both `DEFAULT 'UNKNOWN'`, verified by inserting a minimal row omitting both and reading them back. `exercise_attempts.max_assistance` deliberately defaults to `NONE` instead — recorded here as an intentional asymmetry, not an inconsistency: the app itself creates and fully observes every attempt it starts, so "no assistance used yet" on a fresh STARTED attempt is a real observation, a guarantee `learning_events` cannot make since its `provenance` can be `IMPORT` (reconstructed from a source that never observed assistance at all). `server/src/domain/learning-event.js` (`normalizeLearningEvent`, pure, no DB access, explicit `now` input per design.md's "explicit inputs, clock/cutoff and policy version") is the insert-time validation boundary T30 will call: rejects unknown enum values for kind/outcome/assistanceAvailable/assistanceUsed/assessmentMethod/provenance, rejects non-integer coercion attempts (a numeric-string `sequence`, an object `competencyId` — contrast with the old core's `'[object Object]'` coercion bug, ELC-X10 in context.md), and enforces the same CORRECTION/correctsEventId two-way pairing as the DB CHECK. `server/test/learning-events-schema.test.js` (11/11): table creation, idempotent migration rerun, the NONE-vs-UNKNOWN default asymmetry, duplicate-(attempt,sequence) rejection, the CORRECTION pairing CHECK in both directions plus original-row-untouched proof, cross-user composite-FK rejection, invalid-enum rejection for all four enum columns, and the domain module's own omission/enum/coercion/pairing adversarial fixtures. Full gate: server 219/219 (was 208, +11), root 259/259 (unchanged — server-only change), build not re-run (no client/build-affecting file touched), test:inventory PASS (46 files, was 45, +1 learning-events-schema.test.js). Rust and e2e not re-run — untouched by this task, last recorded values stand (rust 13/13, e2e 34/34). This is schema-only, exactly as scoped: no route, no service, and no attempt/event ever created by product code yet — T30 (capture actual practice assistance) is the next, not-yet-started task. |
| Capture actual practice assistance and item version (T30) | PASS | `server/src/services/attempts.js` (`start`/`useHint`/`revealSolution`/`submit`/`getById`) + `server/src/routes/attempts.js` (`POST /v1/exercises/:id/attempts`, `GET /v1/attempts/:id`, `POST /v1/attempts/:id/hint`, `POST /v1/attempts/:id/reveal-solution`, `POST /v1/attempts/:id/submit`), registered in `server/src/app.js`. `start()` resolves and pins the exercise's CURRENT `exercise_versions` row at that exact instant — never re-resolved — so a later `exercises.edit()` (which only ever appends a new version, T17) cannot retroactively change what an attempt was scored against (AC-18, directly tested by editing the exercise mid-attempt and confirming the submitted event still references the original version id and the original version's content is unchanged). `useHint`/`revealSolution` are the "attributable actions" design.md calls for: each calls a shared `bumpAssistance()` that only ever raises `exercise_attempts.max_assistance` along the fixed `NONE < HINT < PARTIAL_SOLUTION < SOLUTION` order (`ASSISTANCE_LEVELS` from T29's domain module) and never lowers it — verified by re-requesting a hint after the solution was already revealed and confirming max_assistance stays SOLUTION. `submit()` is the discriminating guarantee behind AC-16/AC-17: `assistanceUsed` is READ from the attempt's own tracked `max_assistance`, never accepted from the request body, so "reveal-then-correct" is structurally impossible to record as independent — proven directly (reveal solution, submit CORRECT, assert the persisted `learning_events.assistance_used === 'SOLUTION'`). Both a genuinely-omitted outcome and a genuinely-unassisted attempt resolve honestly and differently: omitted `outcome` stays `UNKNOWN` (via T29's `normalizeLearningEvent`), while `max_assistance` on an attempt this app fully observed and that never called hint/reveal legitimately reads `NONE` (not `UNKNOWN`) — the intentional NONE-vs-UNKNOWN asymmetry T29 already recorded, now exercised end-to-end. Idempotency reuses the exact `checkIdempotency`/`recordIdempotency` pattern from T15/T16: a repeated `(userId, operationKey)` submit returns the identical cached result and creates zero extra events; a resubmit of an already-SUBMITTED attempt WITHOUT a key fails closed with 409 `ALREADY_SUBMITTED` (mirrors `reviews.complete()`'s own established contract) — covers "concurrent/repeated submit is idempotent." A hint/reveal request against an already-SUBMITTED attempt also fails closed (`ALREADY_SUBMITTED`), so a finished attempt can never be reopened or have its recorded assistance altered after the fact. `server/test/attempts.test.js` (10/10): version pinning, archived-exercise rejection, monotonic assistance escalation, reveal-then-correct assistance recording, the NONE-vs-UNKNOWN asymmetry, idempotent/fail-closed submit, edit-after-attempt-start version isolation, cross-user denial across all five operations, late hint/reveal against a SUBMITTED attempt, and a full real-HTTP lifecycle test. Client: `src/remote-store.js` gained an `attempts` namespace (thin `apiRequest` wrappers, REMOTE_MODE-only, no local-mode equivalent — same pattern as `migration-ui.js`) and `src/app.js`'s EXISTING review-exercise reveal/judge UI (the aggregate Q→reveal-A→Acertei/Errei flow already accepted in T16/T22, not a new screen) was wired to lazily start an attempt on first reveal, call `revealSolution` (an already-real, pre-existing "Ver resposta" action) at that same moment, call `useHint` immediately after start when this exercise item's hint text is present (recorded honestly here since this UI shows the hint unconditionally rather than gating it behind its own action — a documented, deliberate scope limit, not a fabricated NONE for a hint the learner could already see), and call `submit` with `assessmentMethod: 'SELF_REPORT'` on Acertei/Errei. Every one of these calls is best-effort and REMOTE_MODE-gated: a failure only logs to console and never blocks the existing aggregate-evidence save path this flow already depends on, per this task's own recovery/rollback instruction ("legacy aggregate practice remains supported"). `e2e/practice.spec.js` (1/1, new): a real browser session creates a subject/unit/exercise via the Cadastro screen, reveals the answer on Hoje's real review-exercise-item, confirms over real HTTP (`GET /v1/attempts/:id`) that revealing already recorded `maxAssistance: 'SOLUTION'` BEFORE any judgment, then clicks Acertei and confirms the attempt reads back `status: 'SUBMITTED'`. Full gate: server 229/229 (was 219, +10), root 259/259 (unchanged), build PASS, test:inventory PASS (48 files, was 46, +1 server test +1 e2e spec), full `npx playwright test` suite 35/35 (34 pre-existing + 1 new practice.spec.js, confirmed no regression in the touched Cadastro/Hoje flow). Rust not re-run — untouched by this task, last recorded 13/13 stands. No competency is ever created or used by this task (competencyId stays optional/null throughout) — competencies exist in the T29 schema but nothing in T30 requires one yet. |
| Reconcile item observations with aggregate results (T31) | PASS | `server/migrations/010-attempt-review-link.sql` adds a plain nullable `exercise_attempts.review_task_id` (no table-level composite FK — SQLite's `ALTER TABLE ADD COLUMN` cannot add one, and recreating the table for one column on a table with no production data was unwarranted risk, same tradeoff T17 already recorded for `exercises.provenance`; `attempts.start()` now enforces ownership AND same-unit for a supplied `reviewTaskId` at the service layer before ever writing it, and the route/client (`remote-store.js`, `src/app.js`'s existing reveal/judge UI) thread the real `task.id` through so a practice attempt is now actually linked to the review it happened during). `server/src/services/review-results.js` (`reconcile`, `correctItemEvent`) is the read+correction side design.md calls for. `reconcile(db, userId, reviewTaskId)` collapses each attempt's `learning_events` to its highest-`sequence` row (a CORRECTION's sequence is always higher than the event it corrects, per T29's own CHECK, so max-sequence is always the live fact — the original row is never touched) and returns item counts, the latest aggregate `learning_evidence` row (if any) for cross-reference, and one `reconciledTotalCount`/`reconciledCorrectCount` pair that is NEVER the sum of both: item-derived counts win whenever any item event exists (`ITEMS_ONLY`/`ITEMS_OVER_AGGREGATE`), the aggregate is the fallback for review-only or pre-T30 completions (`AGGREGATE_ONLY`), and a genuinely empty review reports `NONE` with `null` counts, never a fabricated zero (MX07). `INITIAL_PRACTICE`/`EXTERNAL` evidence (evidence.js) has no `review_task_id` at all, so it is structurally invisible to `reconcile()` — "manual external q/c remains aggregate-only" is a consequence of the schema, not a filter that could be forgotten. `correctItemEvent()` appends one new `learning_events` row (`kind='CORRECTION'`, `corrects_event_id`, next sequence for the same attempt) through the same `normalizeLearningEvent` boundary T29/T30 already use — the original event stays byte-identical (verified directly), and `reconcile()` picks up the corrected outcome on its very next call as one revised observation, not a second one. `server/test/result-reconciliation.test.js` (8/8): items-only, aggregate-only, the discriminating "same practice, exactly once" case (2 items + a matching 2-question aggregate reads back as 2, never 4), assistance visible in `itemEvents`, a genuinely empty review reporting `NONE`, EXTERNAL evidence proven invisible to reconciliation (plus proof creating it makes zero `exercise_attempts` rows), the correction/retry path, and cross-user denial on both functions. `server/test/attempts.test.js` (10/10, re-run — `reviewTaskId` is an optional param, backward compatible) and the full server/root/build/e2e gates were re-run to confirm no regression from the schema/route/client changes. Full gate: server 237/237 (was 229, +8), root 259/259 (unchanged), build PASS, test:inventory PASS (49 files, was 48, +1 server test), `npx playwright test e2e/practice.spec.js e2e/feature-parity.spec.js` 5/5 (targeted re-run of the two suites touching the changed reveal/judge UI and Cadastro exercise flow — no regression). Rust not re-run — untouched by this task, last recorded 13/13 stands. No route/UI exposes `reconcile()`/`correctItemEvent()` yet, exactly as scoped (Where: services + test only) — a dashboard/analytics consumer and a correction-authoring UI are separate, not-yet-started work. |
| Reconstruct a transparent evidence profile (T32) | PASS | `server/src/domain/evidence-profile.js` (`buildEvidenceProfile`, pure — no DB access, no wall-clock reads, explicit `asOf`/`policyVersion`). Retroactively closed a real gap T29/T30 left open first: `learning_events` had no way to record confidence at all even though design.md requires it kept "separate from correctness" — added `server/migrations/011-event-confidence.sql` (nullable `confidence REAL` with a single-column CHECK, which SQLite's `ALTER TABLE ADD COLUMN` does allow, unlike a table-level FK) and threaded it through `normalizeLearningEvent` (T29's domain module), `attempts.submit()`, and `review-results.correctItemEvent()` — verified low/high confidence never changes the persisted outcome. `buildEvidenceProfile()` classifies each post-correction-collapsed event into exactly one of INDEPENDENT_CORRECT / INDEPENDENT_INCORRECT / ASSISTED / UNKNOWN — assistance dominates outcome (an assisted CORRECT is never independent evidence, ELC-X08), and UNKNOWN assistance or outcome is never coerced into a false-certainty bucket (ELC-X01/MX10). Correction/retry is handled by the same max-sequence-per-attempt collapse `review-results.js` already uses (re-implemented locally here since this module is pure and takes no DB rows) — a CORRECTION's sequence is always higher than what it revises (T29's own CHECK guarantees this), so it always wins, and exact-duplicate `eventId`s collapse to one (ELC-X02/MX11). Conflicting independent evidence (an independent CORRECT and a later independent INCORRECT for the same scope) is surfaced as an explicit `hasConflictingIndependentEvidence` flag plus both sides' event IDs — never auto-resolved into one verdict (ELC-X05/X09). Time separation (`delayedIndependentCorrect`) is computed ONLY from real `occurredAt` timestamps between the first observation in scope and each independent-correct event; a forged/attacker-supplied `delayHours`-shaped field on an input event has zero effect, proven directly (MX12) — and no magic 24h/0.8/0.35 threshold is applied or promoted here, since those are explicitly "historical heuristics, not validated constants" that belong to T33's disabled-by-default experimental policy, not this transparent profile. `transferEvidence` is populated ONLY from an explicit `isTransfer:true` on an input event (meant to come from already-reviewed exercise metadata upstream, which does not exist in the schema yet) — never inferred from a distance or string, so it honestly reports zero until that metadata exists. The profile object has NO mastery/mastered field of any kind — directly asserted by reflecting over `Object.keys()` — so "one correct recognition is not mastery" holds structurally, not by convention. `server/test/evidence-profile.test.js` (14/14): empty-input boundary, invalid-`asOf` rejection, basic 4-way classification, ELC-X01/X02/X05/X08 restaged against the new schema, out-of-order/shuffled-input invariance (`deepEqual` on forward vs. reversed input), the correction-supersedes-original path, the no-mastery-field assertion, confidence-independence, the forged-delayHours discriminator, transfer-tagging, and the `asOf` cutoff excluding a future event. `server/test/learning-events-schema.test.js` (+1) and `server/test/attempts.test.js` (+1) cover the new confidence column/param directly. Full gate: server 253/253 (was 237, +16: +14 new evidence-profile tests, +1 confidence validation test, +1 confidence round-trip test), root 259/259 (unchanged — server-only, no client file touched), test:inventory PASS (50 files, was 49, +1). Build/rust/e2e not re-run — untouched by this task (pure domain module, no route/UI/native code), last recorded values stand (build PASS, rust 13/13, e2e 35/35). No route or UI exposes this profile yet, exactly as scoped (Where: domain module + test only) — a consumer (an evidence/analytics screen) is separate, not-yet-started work; T33 (experimental challenger) is the next task and is expected to consume this profile's output, not duplicate its classification logic. |
| Preserve an experimental challenger without promoting it (T33) | PASS | `server/src/domain/experimental/mastery-policy.js` (`evaluateChallenger`) reconstructs the historical mastery/error-hypothesis IDEAS from `context.md`'s audited Branch 2 (`feat/evidence-learning-core-v1`) as a small, versioned, DISABLED-BY-DEFAULT offline/shadow policy — no route, no service, no UI, no schedule/DB write path exists anywhere for it; it consumes T32's `evidence-profile` OUTPUT only (never raw events, never a DB handle), so it structurally cannot write a stored fact even when called. Historical thresholds (24h delay window, 0.8 confidence cutoff, 0.35 low-confidence floor) are preserved as literal, named `PROVISIONAL_THRESHOLDS` constants for traceability — carried forward, not re-derived, not currently consumed as a hard gate anywhere in the module (recorded honestly in the new `pedagogical-validation.md` artifact, not silently glossed over). `CHALLENGER_LABELS` never contains "mastered" or any variant, checked structurally; `evaluateChallenger()` returns a real inert object (not an exception) when `enabled` is false (the default), and when explicitly enabled, `validated` stays `false` unconditionally and a single independent-correct observation never reaches the strongest label (`PROVISIONAL_INDEPENDENT_PATTERN` requires >= `minIndependentCorrectForPattern` (2) independent-correct observations with zero conflicting evidence) — "one correct recognition is not mastery" restaged one layer up from T32's own version of that same guarantee. Conflicting independent evidence always overrides any pattern label rather than being resolved either way. Hypotheses (`NEEDS_MORE_INDEPENDENT_PRACTICE`, `CONFLICTING_EVIDENCE`, `MISSING_OBSERVATIONS_LIMIT_CONFIDENCE`) are grounded ONLY in fields the real schema actually observes (assistance level, conflicting evidence, missing observations) — the old core's procedural/conceptual/motivational "signals" were caller-supplied and never actually collected anywhere in this schema, so this rebuild does not fabricate them; each hypothesis carries its own supporting event IDs, never a bare claim. `server/test/experimental-mastery-policy.test.js` (9/9): the preserved-threshold-constants check, the no-"mastered"-label structural guard, disabled-by-default inertness, the one-correct-is-not-mastery-even-when-enabled case, the pattern-label case at exactly the threshold, conflicting-evidence-always-wins, assisted-only hypothesis, missing-observations hypothesis, and — the discriminating integration proof — a real DB scenario (unit -> exercise -> attempt -> submit) where invoking the enabled challenger against a real-events-derived profile is proven to change zero rows across `review_tasks`/`learning_evidence`/`learning_events`/`exercise_attempts` (before/after snapshot `deepEqual`). `.specs/features/smartlearn-v1-consolidated-v2/pedagogical-validation.md` (new) records `SCIENTIFIC_LEARNING_BENEFIT: NOT_ESTABLISHED`, what is and is not proven, and an explicit 4-step promotion gate (proposal, pre-registered protocol, human authorization, independently-reviewed result) that must be satisfied and linked here before any future PR may claim educational validation. Full gate: server 262/262 (was 253, +9), root 259/259 (unchanged — server-only, no client file touched), test:inventory PASS (51 files, was 50, +1). Build/rust/e2e not re-run — untouched by this task (pure domain module + a markdown artifact, no route/UI/native code), last recorded values stand (build PASS, rust 13/13, e2e 35/35). **PHASE 05 EXIT GATE: SATISFIED.** T29-T33 all proven (commit + validation.md evidence row + tasks.md `[x]` each per task), all required gates green, zero P0/P1 open. Phase 05 (Reconstructed learning evidence) is CLOSED. |
| Secure private PDF upload and source ownership (T34) | PASS | Phase 06's first task. New dependency: `@fastify/multipart@10.1.1` (exact-pinned, matches this codebase's other `@fastify/*` v10-11 plugins on Fastify 5), added because state-changing multipart upload needs real streaming size enforcement (`limits.fileSize`, aborts the read mid-stream on an oversized part rather than buffering it fully first) — no existing dependency in this codebase does that. `server/migrations/012-sources.sql` (plan's suggested "005-sources.sql" renumbered — 005-011 already committed for T15/T17/T26/T27/T29/T31/T32) adds `sources` (owner-scoped, `UNIQUE(user_id, checksum)` for explicit per-user dedup, `status` deliberately left without a CHECK enum since T35 will need to add extraction-outcome values SQLite cannot widen an existing CHECK to hold — same app-layer-enforcement tradeoff already recorded for `exercises.provenance`/`exercise_attempts.review_task_id`). `server/src/services/source-storage.js` (`acceptUpload`/`list`/`getById`) is the actual security boundary: validates the DECLARED content-type AND the real magic bytes (`%PDF-`) — a mislabeled non-PDF is rejected even if `Content-Type: application/pdf` is sent; a lightweight heuristic scan for a `/Encrypt` trailer reference rejects apparently-encrypted PDFs (explicitly documented as a heuristic pre-filter, not an authoritative parse — T35's real PDF.js parse is the authority); the on-disk filename is ALWAYS a fresh random 16-byte hex string, generated server-side, with the user-supplied original filename stored only as sanitized DISPLAY metadata (path separators and control characters stripped by codepoint comparison, never regex hex-escapes, after an authoring-tool artifact briefly wrote literal NUL/control bytes into the source file mid-session — caught immediately by a `grep`/Node byte-level check before it ever reached a test run, and rewritten clean) — a path-traversal payload in the original filename has no filesystem effect by construction, since that string is never used to build a path at all. Quota (`config.sourceQuotaBytes`, default 200 MiB) and per-file cap (`config.sourceMaxBytes`, default 25 MiB per design.md §8) are both checked BEFORE any disk write; a byte-identical re-upload by the same user returns the existing row untouched (explicit dedup, with a `SQLITE_CONSTRAINT_UNIQUE` race-recovery path that deletes an orphaned just-written file and returns the winning row instead of erroring). `server/src/routes/sources.js` (`GET /v1/sources`, `GET /v1/sources/:id`, `POST /v1/sources`) registered inside the same authenticated, CSRF/origin-checked `/v1` context every other mutating route already uses — `@fastify/multipart` is registered on that SAME encapsulated context, so there is no unscoped upload surface anywhere on this server; the upload route declares no `schema.body` (multipart, not JSON) so AJV never touches it. `server/test/uploads.test.js` (12/12): valid-PDF acceptance with random-filename proof, fake-PDF rejection, encrypted-PDF rejection, oversize rejection, path-traversal-payload containment, same-user dedup, cross-user non-conflict, quota enforcement, wrong-declared-content-type rejection, cross-user fetch denial, and two real-HTTP multipart lifecycle tests (successful upload + owner/stranger access split; a non-PDF upload rejected with zero created rows) — every rejection test also asserts zero residual DB rows AND zero residual files, per the task's own discriminating requirement. Full gate: server 274/274 (was 262, +12), root 259/259 (unchanged — no client file touched, T34 has no UI yet), build PASS, test:inventory PASS (52 files, was 51, +1). Rust and e2e not re-run — untouched by this task (server-only, no `src-tauri` or `e2e/`/`src/` files touched), last recorded values stand (rust 13/13, e2e 35/35). No upload UI exists yet, exactly as scoped (Where: routes + service + migration + test only) — src/source-proposals-ui.js is T36's job; PDF text extraction (page provenance, parser worker) is T35's, not started. |
| Extract PDF text with page provenance under limits (T35) | PASS | New pinned dependency `pdfjs-dist@6.3.289` (requires Node >=22.13/24, matches this project's Node 24 target; the `legacy/build/pdf.mjs` entrypoint is the Node-compatible build, used for text-only extraction — no canvas/rendering path ever touched). `server/src/pdf/extract-worker.js` runs inside a `node:worker_threads` Worker, NEVER on the main server thread: reads only the exact file path it was given, calls `getDocument` with `cMapUrl`/`standardFontDataUrl` left undefined (no network fetch is ever attempted) and `isEvalSupported: false` (disables pdf.js's optional eval-based font-program path — no code from inside a PDF is ever evaluated as JavaScript; plain `getTextContent()` never triggers PDF-embedded JS actions in the first place, since that only happens through a separate scripting/annotation manager this code never touches). `server/src/pdf/classify-extraction-error.js` is a small pure function mapping a pdf.js exception to one of our explicit statuses (`PasswordException`->`ENCRYPTED`, everything else->`EXTRACTION_FAILED`) — kept separate and directly unit-tested with synthetic errors, since constructing a real, correctly-encrypted PDF fixture (genuine RC4/AES per the PDF spec) would be disproportionate to what this mapping itself needs to prove. `server/src/services/source-extraction.js` (`extractSource`/`listPages`) is the orchestration/bounding layer design.md calls for: spawns the worker with `resourceLimits.maxOldGenerationSizeMb` (memory bound) and a `setTimeout`-based deadline that calls `worker.terminate()` and resolves `TIMEOUT` if the worker hasn't answered in time — the async worker call happens BEFORE the (synchronous, better-sqlite3) `db.transaction()`, matching design.md's "network calls and async... PDF work occur outside them." A successful extraction atomically replaces `source_pages` (DELETE + re-INSERT in one transaction, since this is a derived/recomputable projection, never the historical record — heritage.md H-06 pattern) and records `extraction_status`/`parser_version`/`page_count`/`extracted_at` on `sources`; ANY other outcome (`EXTRACTION_FAILED`/`TIMEOUT`/`ENCRYPTED`/`IMAGE_ONLY_OR_UNREADABLE`) only ever updates that one status column — the original uploaded file (T34's) is never opened for writing by any code path here, verified directly (byte-identical before/after a failed extraction). `server/migrations/013-source-extraction.sql` adds those columns (no CHECK enum on `extraction_status`, same tradeoff already recorded for `sources.status`/`exercises.provenance`) plus `source_pages` (owner-scoped, `UNIQUE(user_id, source_id, page_index)`, 1-based `page_index` matching pdf.js's own numbering). `server/test/pdf-fixtures/build-fixture-pdf.js` builds a real, well-formed multi-page PDF entirely in code with byte offsets computed programmatically (never hand-counted) — verified directly against the real pdf.js parser before being trusted as a fixture; a real bug was found and fixed WHILE building it (not a pre-existing product bug): a too-narrow `MediaBox` silently truncated `getTextContent()`'s returned string at the page's visible boundary — reproduced directly (isolated from the worker, direct pdf.js call) by testing the same content against a wide vs. narrow MediaBox before concluding the fixture's page geometry, not this task's extraction code, was at fault; the fixture now uses a comfortably wide MediaBox and this is documented in the fixture file itself. `server/test/pdf-extraction.test.js` (7/7): the error-classifier unit tests, a real multi-page fixture preserving exact text AND exact page-index mapping with checksum/parser-version recorded, a deliberately truncated (malformed) PDF failing extraction explicitly with the original file byte-identical after, a 1ms-deadline test proving the worker is actually terminated and the caller gets a prompt `TIMEOUT` result rather than hanging, idempotent re-extraction (no duplicate page rows), cross-user denial on both `extractSource` and `listPages`, and rejection of a nonexistent source id. Full gate: server 282/282 (was 274, +8), root 259/259 (unchanged — no client file touched), test:inventory PASS (53 files, was 52, +1). Build/rust/e2e not re-run — untouched by this task (server-only, no `src-tauri`/`e2e/`/`src/` files touched), last recorded values stand (build PASS, rust 13/13, e2e 35/35). No route exists yet, exactly as scoped (Where: worker + service + test only) — T36 (source-to-unit proposals) is the first consumer of `listPages()`, not yet started. |
| Import/export/data-loss discrimination | NOT_RUN | |
| Unit creation + fixed scheduling transaction (T15) | PASS | shared/review-schedule.js extracted as the single pure source (16 fixed offsets, design.md §4 verbatim); src/review-schedule.js becomes a compatibility re-export — client tests unaffected (9/9). Found and fixed a real calendar-validation bug while writing the leap-day test: `new Date("2026-02-30")` silently overflows to 2026-03-02 in JS instead of failing, so a study date like Feb 30 was being silently reinterpreted as a different date rather than rejected — added an explicit round-trip check (format the Date back to YYYY-MM-DD and compare to the input) that catches this for both server and client (same shared file). Added 3 new client tests proving Feb 30 rejected, Feb 29 accepted in a real leap year (2028) and rejected in a non-leap year (2026). server/src/services/learning-units.js: subject resolve-or-create + unit + exactly 16 review_tasks in ONE db.transaction() — resolveOrCreateSubject issues raw SQL rather than calling services/subjects.js (which isn't itself transaction-aware) specifically so the whole operation commits or rolls back atomically. server/migrations/005-operation-idempotency.sql + server/src/services/idempotency.js: checkIdempotency() is a plain read done BEFORE opening the transaction; recordIdempotency() is called INSIDE the same transaction as the actual side effects — deliberately not a single wrapper function, because wrapping the operation and recording it as two separate steps would reopen exactly the "injected failure leaves partial state" gap this task exists to close. server/test/create-unit.test.js (10/10): exactly 1 subject + 1 unit + 16 review_tasks created; reusing an existing subjectId creates no new subject; due dates verified exact across a real leap-year boundary (2028-01-15 +30/+60 days spans Feb 29 correctly) and across a UTC year boundary independent of test-runner local timezone; a title-too-long validation failure creates zero rows (fails before the transaction opens); an invalid calendar date creates zero rows; a mid-transaction failure (nonexistent subjectId, which fails INSIDE resolveOrCreateSubject after title/date validation already passed) leaves zero unit/review rows; same operationKey + same payload returns the identical original result and creates nothing twice; same operationKey + different payload throws IdempotencyConflictError (409) rather than silently doing something else; full HTTP round trip confirms "exactly one of subjectId or newSubjectName" is enforced (both or neither -> 400) and a valid create returns 201 with reviewCount=16. Full gate: server 132/132, root 240/240, rust 13/13, build PASS, full e2e 17/17, test:inventory PASS (31 files). |
| Real PDF/source fidelity | NOT_RUN | |
| AI fake-provider contract | NOT_RUN | |
| Configured real-provider integration | NOT_RUN | Requires credentials/consent |
| Offline account isolation/cold start | NOT_RUN | |
| Windows build and actual UAT | NOT_RUN | |
| Android build and actual UAT | NOT_RUN | |
| Reminders per supported runtime | NOT_RUN | |
| Accessibility/pt-BR | NOT_RUN | |
| Operational restore with source assets | NOT_RUN | |
| Material mutations MX01-MX21 | NOT_RUN | |
| Formal spec/task/completion validators | NOT_RUN | |
| Final independent verifier | NOT_RUN | |
| Authorized real-data migration | NOT_RUN | External authorization required |
| Authorized deployment/store publication | NOT_RUN | External authorization required |

## Required final report

Exact HEAD/tree, task counts and IDs, source/build environment, every required gate, open defects by impact, deferred items, uncommitted files preserved, external permissions still needed, independent verifier scope, next action, and distinct IMPLEMENTATION_COMPLETE / V1_VALIDATED / PRODUCTION_RELEASED.

A blocked final verification is not permission to erase unverified criteria or invent alternative scope. Preserve code/evidence and produce a resumable terminal checkpoint.
